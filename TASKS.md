# Tasks

Atomic, trackable work items. **Findings documents do not carry their own to-do
lists** -- an audit says what is true, this file says what to do about it, and
the two are kept apart so a finding can be read without being re-litigated and a
task can be closed without editing the argument that produced it.

Every task cites the document that justifies it. If a task has no source, it is
not yet a finding and probably needs one.

Status: `open` | `doing` | `blocked` | `done` | `wontfix` (with a reason).

## Conventions

- IDs are stable and never reused. Prefix by area: `LITH` lithology, `GRAV`
  gravity, `VOLC` volcanism and weathering fluxes, `SURF` derived surface
  classes, `CLIM` climate, `HYD` hydrography, `BIO` biosphere, `MIN` economic minerals,
  `DUST` the aeolian component, `REF` references and provenance, `CONS`
  consistency checking, `PHYS` physics calibrated for the wrong world, `BUDG`
  the error budget and what it is denominated in, `SPEC` the stellar spectrum
  and how the radiation scheme integrates it.
- One line per task. If it needs a paragraph, it needs a findings document.
- A task that turns out to be wrong is closed `wontfix` with the reason, not
  deleted. The reasoning is the point.
- A closed task moves to `archive/tasks.md` rather than staying here, so this
  file shows only what is left. Ids are never reused, so a cited id that is not
  here is there.

---

## Open

| id | task | source | status |
| --- | --- | --- | --- |
| SURF-6 | Nothing consumes the derived surface classes yet. Two consumers were the reason for building them and both are one field away: `aeolian/scripts/build_dust.py` should read `pavement` as an emission SUPPRESSOR and a store of deposited dust, and `pedology/scripts/phosphorus_budget.py` should read `pavement` as a P sink and `diatomite` as the deflatable source Hudson-Edwards measured | `pedology/notes/derived-surface-classes.md` | open. The pavement sign is the whole point of the Wells et al. (1995) correction: as "deflation armour" the class would have been a product of emission and increased with it, and as pavement it suppresses emission and stores what falls. Leaving the field unread keeps that correction theoretical. Do it AFTER the carve, since both consumers would be re-run on the new terrain anyway |
| SURF-4 | Diatomite is placed against a STATIC lake proxy, not lake occupancy over the stellar cycle. The strandline band between the solved lake surface and the spill level is the ground a single climatology's lake COULD vacate; the design asks for the band it is measured to vacate over the 57-year component, which is the forcing slow enough for lake level to equilibrate | `pedology/notes/derived-surface-classes.md` | open, and blocked on CYC-1 rather than on effort. It cannot be faked from one climatology: a basin permanently full never exposes its bed and a basin permanently dry never accumulates one, so the whole class is about alternation. Re-run `build_surface_classes.py` against the cycle climatologies when they exist |
| SURF-5 | The pavement clast-supply criterion has no source. `surface_classes.yaml` stands in "a consolidated bedrock substrate rather than basin fill" and declares it as a stand-in; it is the weakest rule in the file, and pavement runs from 0.47% to 9.57% of land across the aeolian roughness bracket, so the rule is not the dominant uncertainty but it is the only undeclared-in-literature one | `pedology/notes/derived-surface-classes.md` | open, small. Wells et al. (1995) is read and settles the MECHANISM (pavements are born at the surface, dust accumulates beneath the mosaic) but gives no clast-supply threshold. McFadden et al. (1987) is held and unread and is the obvious next source |
| MIN-4 | What is left DECLARED in the downstream prospectivity rules after the 2026-08-17 literature pass: the wet end of the supergene copper window, and any relief or slope term for bauxite or nickel laterite. Both sources require the relief condition qualitatively and neither puts a number on it | `notes/economic-minerals.md`, `references/INDEX.md` | open, and narrower than it was. Bauxite, Ni laterite and supergene's DRY end are now sourced; the placer rule's two missing thresholds are `sourced-negative`, quoted from papers that say the quantity is unestablished. The relief omission is one-signed: both deposits are placed on steep ground where erosion would strip them. Sillitoe (2005) `10.5382/AV100.22` and Freyssinet et al. (2005) `10.5382/AV100.21` would close most of the rest and BOTH are unobtainable -- SEG serves 403 for the Economic Geology 100th Anniversary Volume and no mirror carries it |
| MIN-5 | Lithium and borate prospectivity rest on a geological association, not on a modelled mechanism. Neither element is among Meybeck's eight species, so unlike soda and gypsum they cannot be derived from the chemical divide at all, and boron in Earth's borate basins comes substantially from hydrothermal and fumarolic input which this project does not model | `notes/economic-minerals.md` | open, and possibly `wontfix`: closing it properly needs a Li and B release table per lithology, which Meybeck does not provide, plus a hydrothermal flux this pipeline has no way to place. The honest outcome may be to keep the fields with `grounding: declared` and stop there. Decide deliberately rather than by attrition |
| REF-7 | `relative_to(PROJECT_ROOT)` is still called directly in ten files, where `lib/paths.py:rel` exists precisely because that call RAISES on a path outside the tree. Fixed in `pedology/scripts/weathering_fluxes.py`, which is where it was hit; the pattern remains in biosphere, hydrography, exoplasim, maps, minerals and two more pedology scripts | `notes/failure-modes.md` class 1 | open, small and mechanical. This is class 1 exactly: one quantity, several consumers, a fix that reached some of them. It is LATENT in the main tree, since nothing there resolves outside the root, and it fires in a git worktree where the ignored payloads are symlinks -- so it will keep being rediscovered one script at a time until it is swept |
| REF-8 | `aeolian/analysis/dust_baseline.nc` and `hydrography/data/<build>/surface_water.nc` carry no `vesper_source_build` attribute, so `lib/provenance.py:require_build` warns instead of checking and every consumer of either is trusting a filename | `notes/failure-modes.md` class 4, `lib/provenance.py` | open. Both stamp a build under other names or not at all -- `dust_baseline.nc` has `source_build` without the `vesper_` prefix `require_build` looks for. Two one-line changes plus a regeneration each. Left alone here because the dust chain and the lake solution are being worked on elsewhere and a regeneration would collide |
| BIO-1 | Regenerate the LPJ-GUESS driver: `vesper_driver_provenance.json` is pinned to a superseded config and `check_consistency.py` has been failing on it | `scripts/check_consistency.py`, `WORKFLOW.md` A3 | deliberately DEFERRED until after iteration 2's baseline run. The driver is built from a climatology and a soil that the carve replaces, so doing it now is make-work whose only effect is silencing a warning that is currently telling the truth: no biosphere inputs exist for this build. The orbit-dependent half was durable and is done (BIO-3) |
| CYC-1 | Run the stellar cycle off the settled baseline. `WORKFLOW.md` section A2 requires it AFTER a converged baseline and BEFORE the carve verdict, and nothing was tracking it: carving is irreversible, so the terrain ratchets toward the cycle's wet extreme and a verdict taken on the mean climate systematically under-carves. The run also measures the damping factor, known only as 0.24 to 0.6, which converts any future flux amplitude into a climate without another run | `WORKFLOW.md` section A2, `config/planet.yaml` stellar_cycle | open, and ON THE CRITICAL PATH. The cycle binary is built and verified. Length is set in periods of the LONG component, 57 Earth years, not the medium one. The carve list of 2026-08-17 was taken on the mean climate and should be re-taken after this |
| GRAV-6 | Glacial erosion carries no gravity term, and Glen's law makes ice velocity go as `g^3` (2.23x here) | `notes/audits/orogen-gravity.md` | blocked on the downscaling pass, deliberately. The audit's recommendation was Option B, carry it as a declared gap: `iceFlow` is a heuristic accumulation rather than a Glen's-law velocity, so a rigorous g^3 on it is precision theatre. Decisive on top of that, a glacial valley is 1-5 km against a 15.19 km mesh cell, so the process is SUB-GRID and cannot be resolved here at any gravity. It belongs with the downscaling machinery, which has to persist sub-grid hypsometry anyway |
| DUST-11 | Settle the CATCHMENT half: run one prescribed-dust climate segment and measure what it does to precipitation and runoff. Runoff is the carve criterion's denominator and only 19% of land precipitation, so it amplifies; absorbing aerosol suppresses precipitation through a fast adjustment that no offline calculation can reach, because it is a response of the circulation and not of a surface energy balance | `notes/dust.md`, `aeolian/notes/prescribed-dust-run.md` | PATCH APPLIED and built 2026-08-17 with `ndustrad = 0`, so every binary carries prescribed dust and no run sees it yet. That is the point of residency: the dust run and its control now come from the SAME executable, which is the only way the comparison measures dust rather than a rebuild. What remains is the run itself, specified in `aeolian/notes/prescribed-dust-run.md`: a matched pair of 40-orbit segments, about 4 h, six gates, and a prediction of 50 to 250 basins of over-carve made before it |
| DUST-12 | The offline forcing and the in-model aerofile use DIFFERENT refractive indices for the same dust. `exoplasim/scripts/dust_forcing.py` computes everything from OPAC; the aerofile uses the measured indices `aeolian/config/dust.yaml` selects, giving single-scattering albedo 0.9666/0.9750 against OPAC's 0.8942/0.9515. Weighted, that is an absorption optical depth about 0.40x OPAC's, so the in-model shortwave absorption should come out roughly 2.5x smaller than `analysis/dust_forcing.json` and `analysis/dust_surface_forcing.nc` say | `aeolian/config/dust.yaml`, `exoplasim/scripts/dust_forcing.py` | open, and it is an INPUT CHOICE rather than a defect in either: two defensible optical datasets, and the project has to pick one and let both consumers read it. Same shape as PHYS-2. It matters now because DUST-11's run is gated on a longwave term whose size is set by absorption, and because the offline surface forcing is what DUST-10's 23-basin lake result was computed through. `dust.yaml` is the file that should decide. Found while preparing DUST-11 |
| DUST-3 | Put the emission scheme in the ExoPlaSim fork, keeping the source map outside it as boundary fields. DECIDED; DESIGNED in `aeolian/notes/in-model-dust.md`, which is detailed enough that landing it is mechanical | `aeolian/notes/in-model-dust.md`, `notes/dust.md` | open. "Replace one line" was wrong: six pieces in order -- the three upstream defects, replace the bottom-level sink with a deposition velocity, wet scavenging, boundary fields plus Kok 18, the longwave aerosol term, and size bins only if needed. The longwave is the dominant RISK (inside the radiation solver) and `aerocore` running serial on NROOT is the item most likely to be UNDERESTIMATED, since nothing the emission needs is gathered. Five or six patches to a compiled model, each needing a rebuild per CLAUDE.md rule 4. SEQUENCING unchanged: `WORKFLOW.md` A3, its own iteration, after iteration 2's baseline run |
| DUST-7 | There is NO wet deposition in ExoPlaSim's aerosol. `aerocore` takes no precipitation argument of any kind -- gravitational settling is the only removal, plus a bottom-level sink that is deliberate but not physical -- 99% per timestep, an implied deposition velocity of 52.7 cm/s against a real 0.1-2, and it does not scale with dt. See `aeolian/notes/in-model-dust.md` | `notes/dust.md` | open, part of DUST-3 and designed in `aeolian/notes/in-model-dust.md`. Mechanically the easiest of the physics additions: `dprl` and `dprc` exist, they are simply not handed over. `aeolian/README.md` says of our own chain that without the wet term the fine mode's lifetime is out by an order of magnitude, so porting emission in-model without adding scavenging would lose a first-order sink. Needs a loss term proportional to `dprl + dprc`, which is the same Sportisse (2007) route the offline chain already uses |
| DUST-8 | One aerosol, one radius, one density. `plasimmod.f90:99` fixes `parameter(NAERO = 1)`; `apart = 50e-9` m and `rhop = 1000` kg/m3 are photochemical haze against mineral dust at 0.1-20 um and 2650. The `do jc=1,NAERO` loops exist, but `apart`/`rhop` are scalars shared by every bin and `mmr2n` takes them as scalars | `notes/dust.md` | open, part of DUST-3 and designed in `aeolian/notes/in-model-dust.md`, which recommends the single effective radius first (0.98 um, already in the aerofile) with the offline chain kept for the size-resolved deposition loess and phosphorus need. Either promote size and density to per-bin arrays with per-bin optics, or pick one effective radius reproducing the mass extinction efficiency in `analysis/dust_optics.json` and declare what the single mode costs deposition |
| HYD-3 | Measure the overshoot: re-evaluate an already-carved set against the climate that carving produced, and report how many would no longer have carved | `WORKFLOW.md` section 4 | blocked -- it applies to a verdict taken on carved terrain, and a pre-carve base restarts the count at iteration 1 |
| CLIM-7 | Decide whether the convergence criterion should test the REPORTED top-of-atmosphere net or the storage the model state actually shows. The window question is answered: `assess_convergence.py` takes the trailing `--window` orbits and has no other window to take, and the 0.0014 was orbits 51-60 at run length 61, stale before the commit that quoted it finished re-measuring it. What it exposed is worse -- the criterion reads a diagnostic offset by -0.573 W/m2, so every recent miss on this baseline is smaller than the offset and has the offset's sign | `exoplasim/notes/baseline-equilibration.md` | open. The threshold and the quantity have to be fixed BEFORE the answer is looked at and the answer has now been looked at, so this is a deliberate decision rather than a retrofit; `WORKFLOW.md` section 7. Until it is taken the baseline stays `quasi_equilibrated`, missing `abs_mean_toa_lt_0.5_w_m2` by 0.1010 on its recorded window and 0.0995 on its current one. SETTLED for A2: equilibration is not what holds the cycle run back, and more orbits cannot move a number the planet does not have |
| CONS-3 | A run stages its stellar spectrum BY NAME and records only the name. So editing `star.spectral_type` and regenerating `k25v.dat` in place changes what the radiation sees while the resume guard's allowlist calls `spectral_type` inert, and `continue_exoplasim.py` resumes across it without complaint | `lib/provenance.py`, `scripts/check_consistency.py` | open, small, and found while closing CONS-1. The biosphere half is already safe -- `vesper_provenance.json` records `spectrum_sha256` -- so the fix is to make a run manifest do the same and compare it, not to invent a mechanism. Note the shape: the allowlist entry is CORRECT about the config key and wrong about the artifact, because the value reaches the model through a file the key only names. WORKFLOW.md loop C is where this bites, since it moves luminosity and effective temperature and says to re-run `build_stellar_spectrum.py` past 2 to 3% |
| CLIM-5 | Patch PlaSim's low-I/O output path, which writes a corrupt first record per model call. Runs now set `NLOWIO = 0` to avoid it at 25x the output volume, so this buys back disk rather than correctness | `exoplasim/notes/first-output-bin.md` | APPLIED and built 2026-08-17, and NOT yet done. The patch is resident and compiled into all five binaries, so the corrupt first record should be gone -- but nothing has demonstrated that, because every run since has used `NLOWIO = 0`, which is the workaround the patch exists to retire. What closes this is one short segment at `NLOWIO = 1` whose first output record agrees with the `NLOWIO = 0` reference to the precision the rest of the record does. Until then the fix is asserted rather than shown, which is the standard this project holds elsewhere. It also changes the RESTART LAYOUT, so a run started before the rebuild cannot be resumed by these binaries |
| CLIM-6 | Give `denergy` an accumulator in `outmod.f90` beside `ashfl`, `alhfl` and the rest, so the 28 energy terms are time means like every other field on the stream rather than instantaneous values at the output step | `exoplasim/notes/water-and-energy-closure.md` | APPLIED and built 2026-08-17, unverified for the same reason as CLIM-5: `denergy` now has an accumulator beside `ashfl` and the rest, but no run has written one. Confirm on the first segment that the 28 terms are time means rather than instantaneous values by checking one term against its own accumulated flux. Deliberately adds NO restart records, because `get_restart_array` stops on a missing name and persisting the accumulator would make these binaries refuse every restart the old ones wrote |
| PHYS-1 | Re-weight the Lacis & Hansen SHORTWAVE water vapour absorptance for the K dwarf, as `o3uvw`/`o3visw` already do for ozone. It gives absorptance as a fraction of total incident SOLAR flux, so the Sun's spectrum is inside the number 2.9 and `radmod.f90`'s division by `zsolar2` cancels against the band-2 flux it multiplies, leaving a K dwarf with the Sun's absorbed fraction | `notes/audits/physics-review.md`, `exoplasim/notes/shortwave-water-vapour.md` | PATCH APPLIED and built 2026-08-17, and the physics is still OFF: `h2osww` defaults to 1.0, so the binaries reproduce the runs that exist. Deriving the weight is DONE -- 1.346 against k25v, bracket 1.301 to 1.363, from the Howard/Burch/Williams band absorptances rather than a flux share. What is left is a DECISION, not work: setting `h2osww` moves atmospheric shortwave absorption by +15.0 W/m2 and the mean by a predicted +2.49 K, which takes the world off its design mean and re-derives the flux to about 0.932. It also cannot be attributed if it lands in the same run as the spectrum declaration; two short segments with the weight off and on would separate them |
| PHYS-6 | ExoPlaSim has NO shortwave CO2 absorptance. `radmod.f90` carries CO2 only in `lwr`, from Sasamori (1968); `swr` has ozone in band 1 and water vapour in band 2 and nothing else. Faithful to Lacis & Hansen, who did not parameterise it either, but a K dwarf puts 1.5x the Sun's share of flux in the CO2 near-infrared bands | `notes/audits/physics-review.md`, `exoplasim/notes/shortwave-water-vapour.md` | open. Came out of PHYS-1 and is the half of it that could not be closed, because there is no term to re-weight. Worth **2.5 W/m2** of atmospheric shortwave absorption here against 1.75 solar-weighted, integrated from the same Howard band data and charged only with what water vapour leaves it. The 1.75 is the check: Earth's near-infrared CO2 solar absorption is measured at 1.5-2.5 W/m2, which this derivation did not choose. Same sign as PHYS-1 and about a sixth of its size, so it compounds rather than cancels. This is a NEW ABSORBER, not a weight: a namelist key, a column integral, a transmissivity term in band 2 and a decision about the 2.7 um overlap. Land it in the SAME rebuild as PHYS-1 or pay for a second one |
| SPEC-1 | Re-run the baseline on the spectrum the config declares. Every orbit after the first of every run on this build was integrated against a 4965 K blackbody, worth +0.024 on broadband snow albedo and moving every derived surface albedo with it | `notes/audits/physics-review.md` finding 2 | blocked on `SPEC-2`. The driver bug is fixed and `verify_stellar_spectrum` now refuses a run whose namelist lacks the configured spectrum, but staging the spectrum without `SPEC-2` swaps a 0.024 albedo error for a 3.4x Rayleigh one. Nothing in the repo derived from a run is safe to quote as spectral until this lands: the band-2 share, the snow, ice, glacier, ground and ocean albedos and `rcoeff` are all blackbody values |
| SPEC-2 | Patch `solarini` so its 5772 K Rayleigh reference is integrated on its own grid. `radmod.f90:224-226` tabulates `bbg1`/`bbg2` on the model grid, lines 230-233 then overwrite `wv1`/`wv2` with the spectrum file's wavelengths, and lines 287-299 integrate the reference over the wrong ones. `rcoeff` comes out 0.2097 for k25v against 0.7125 on one grid | `notes/audits/physics-review.md` finding 2 | open, and it gates `SPEC-1`. The fix is to keep the reference wavelengths in their own arrays; the blackbody branch is unaffected because nothing overwrites the grid there. `lib/stellar.py:rayleigh_coefficient` reproduces all four values including the 5772 K identity. Same standing caution as every `radmod.f90` change: it is a patch to a compiled model and every binary needs rebuilding, so land it with `PHYS-1`'s |
| CLIM-1 | Name the residual between the top-of-atmosphere and surface budgets. It is a constant offset of -0.573 +/- 0.035 W/m2 in the REPORTED TOA net, measured against the prognostic state on eight windows across four runs spanning 7.8 K of mean temperature, with four fifths of it in the atmospheric column rather than at the surface | `exoplasim/notes/water-and-energy-closure.md`, `exoplasim/scripts/close_state_energy.py` | open, and down to one candidate set. ELIMINATED: the 28-term decomposition, which closes to 0.02 W/m2; a storage term in the slab or the sea ice, since the planet's heat content drifts -0.055 W/m2 against a claimed -0.600 and the mixed layer drifts 0.0038 K/orbit against the 0.0769 the TOA implies; and the incoming shortwave, since `rst - rsut` matches `GSOL0/(4 sqrt(1-e^2))` to 0.012 W/m2. LEFT: `rsut`, `rlut`, or an atmospheric heating neither flux diagnostic books. Separating them needs the radiative transfer recomputed offline from the run's own temperature and humidity profiles, not more model time |
| BIO-2 | Quote productivity with the `nfix_a` bracket 0.102-0.367 carried through rather than the central value alone; the span is 18.2% of NPP and it is the largest nitrogen lever | `biosphere/notes/productivity-prediction.md` | blocked on iteration 2's baseline run -- no LPJ-GUESS run exists on this build |
| HYD-16 | Rebuild `surface_water.nc` and the lake compositing in albedo 174-176 before the next baseline run, and re-render the basemap | `notes/audits/carve-criterion-terms.md` finding 8, `notes/audits/grid-convention-and-runoff.md` finding 1 | open. The CODE is fixed, the ARTIFACTS are not. `surface_water.nc` on disk was solved with every basin's lake precipitation and open-water evaporation taken from its antipode, and with the land-rate floor in place, so both the lake areas and the -0.018 they are worth on land-mean albedo are wrong. It is not regenerated here because regenerating it without re-running the climate would pair a corrected surface field with a climatology built from the uncorrected one. It belongs in the baseline re-run `grid-convention-and-runoff.md` already forces, before CYC-1. The rendered basemap is stale for the same reason and is cheap |
| HYD-17 | Decide whether the 1,062 basins whose catchments deliver no runoff are a real population or the length of the climatology window | `notes/audits/carve-criterion-terms.md` finding 4 | open, and cheap to settle. A land cell cannot have a negative annual `P - E` here: the mask is binary, routed runoff never re-enters the evaporating bucket, and the store is periodic at steady state. It nonetheless shows on 1,597 of 4,105 land cells with a mean of -3.1 mm/yr, which is the residual non-periodicity of a five-orbit window -- land-mean drift 8.6 mm per bin. Those cells concentrate at high latitude, which is where the dry catchments are and where the seasonal store is largest. A longer seasonal window costs orbits rather than physics and would say whether that population is 1,062 or several hundred fewer |
| BUDG-1 | Measure this world's hydrological sensitivity: one perturbation run reporting dP and dE per kelvin, giving a conversion from the error budget's kelvin into runoff. Runoff is 15.5% of land precipitation, so it amplifies dP by 6.5x and dE by 5.5x, and the carve verdict reads runoff while every budget item is priced in kelvin | `notes/audits/missed-couplings.md` | open. The single measurement that lets the existing budget rank anything against the carve, which is the one irreversible output |
| BUDG-2 | Re-express `analysis/error_budget.json` with a second currency alongside kelvin: effect on runoff and on basin count | `notes/audits/missed-couplings.md` | machinery done 2026-08-17, still blocked on BUDG-1 for the one conversion it cannot measure offline. The budget now carries the measured land water balance, its amplification factors, and the basin response to precipitation, land evaporation and lake evaporation reconstructed from the carve list's own criterion. `HYDROLOGICAL_RESPONSE_PER_KELVIN` is the single declared input; setting it fills every per-item column |
| BUDG-5 | The error budget's basin reconstruction no longer reproduces the verdict: `analysis/error_budget.json` reports "MISMATCH: 2026 against 2032" where it used to reproduce the recorded overflow count exactly. It re-derives each basin's precipitation and lake evaporation from its aridity index, evaporation margin and critical index, and the criterion those were recorded under has since changed -- the Penman floor removed, the 2 m against 300 m level mixing fixed, and the sill slope carried explicitly | `analysis/error_budget.json`, `notes/audits/carve-criterion-terms.md` | open, and the check working is the point: it was built to reproduce the count exactly so that a drift like this could not pass unnoticed, and six basins in 2032 is what it caught. Re-derive the reconstruction against the current criterion, or read the fields the verdict now records directly rather than inverting them. Until then the `carve_currency` block's basin numbers are approximate at the few-basin level, which does not change any ranking |


## Closed

Moved to `archive/tasks.md`, with the reasoning that closed each one. Ids are
never reused, so look there when a commit cites an id this file does not have.
