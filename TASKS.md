# Tasks

Atomic, trackable work items. **Findings documents do not carry their own to-do
lists** -- an audit says what is true, this file says what to do about it, and
the two are kept apart so a finding can be read without being re-litigated and a
task can be closed without editing the argument that produced it.

Every task cites the document that justifies it. If a task has no source, it is
not yet a finding and probably needs one.

Status: `open` | `doing` | `blocked` | `done` | `wontfix` (with a reason).

## Conventions

- IDs are stable and never reused. Prefix by area: `LITH` lithology, `GRAV`
  gravity, `VOLC` volcanism and weathering fluxes, `SURF` derived surface
  classes, `CLIM` climate, `HYD` hydrography, `BIO` biosphere, `MIN` economic minerals,
  `DUST` the aeolian component, `REF` references and provenance, `CONS`
  consistency checking, `PHYS` physics calibrated for the wrong world, `BUDG`
  the error budget and what it is denominated in.
- One line per task. If it needs a paragraph, it needs a findings document.
- A task that turns out to be wrong is closed `wontfix` with the reason, not
  deleted. The reasoning is the point.
- A closed task moves to `archive/tasks.md` rather than staying here, so this
  file shows only what is left. Ids are never reused, so a cited id that is not
  here is there.

---

## Open

| id | task | source | status |
| --- | --- | --- | --- |
| MIN-2 | Place the weathering, drainage and brine deposit types downstream: bauxite, laterite Ni, supergene Cu, placers, and the brine evaporite minerals | `notes/economic-minerals.md` | unblocked: the baseline climatology exists. The brine half is close to free: `brine_paths.py` already solves the chemical divide per basin, which is what decides the mineral |
| VOLC-3 | Place silcrete and diatomite against per-basin silica supply. A large share of silica delivery goes to closed basins, which is the setting for both, but per-basin placement needs hydrography run on the arc-bearing build | `pedology/notes/derived-surface-classes.md` | unblocked: hydrography and a climatology both exist on this build |
| BIO-1 | Regenerate the LPJ-GUESS driver: `vesper_driver_provenance.json` is pinned to a superseded config and `check_consistency.py` has been failing on it | `scripts/check_consistency.py`, `WORKFLOW.md` A3 | deliberately DEFERRED until after iteration 2's baseline run. The driver is built from a climatology and a soil that the carve replaces, so doing it now is make-work whose only effect is silencing a warning that is currently telling the truth: no biosphere inputs exist for this build. The orbit-dependent half was durable and is done (BIO-3) |
| CYC-1 | Run the stellar cycle off the settled baseline. `WORKFLOW.md` section A2 requires it AFTER a converged baseline and BEFORE the carve verdict, and nothing was tracking it: carving is irreversible, so the terrain ratchets toward the cycle's wet extreme and a verdict taken on the mean climate systematically under-carves. The run also measures the damping factor, known only as 0.24 to 0.6, which converts any future flux amplitude into a climate without another run | `WORKFLOW.md` section A2, `config/planet.yaml` stellar_cycle | open, and ON THE CRITICAL PATH. The cycle binary is built and verified. Length is set in periods of the LONG component, 57 Earth years, not the medium one. The carve list of 2026-08-17 was taken on the mean climate and should be re-taken after this |
| HYD-15 | The retain mapping treats sill incision as SLOPE-INDEPENDENT: `S^n` is absorbed into `C`, so depth enters only as the amount of rock to remove | `hydrography/notes/retain-fraction.md`, `notes/audits/carve-criterion-terms.md` | done 2026-08-17, and it was a different mapping rather than a coefficient. The saddle's own slope is zero by construction, but the OUTFLOW CHANNEL below it is in the export: ten receiver steps from `spill_exit_region` covers a median 135 km. Measured there, `log S` against `log depth` gives r = 0.735 with an exponent of 1.04 by OLS and 1.41 by RMA, and r = 0.072 against discharge, so it is real and it is not a proxy for the water. At n = 1 the effective depth exponent is +0.02 and the depth cancels, as predicted. `S` is now carried explicitly, normalised by the population geometric mean. Counts barely move (1598 -> 1608 carve) and the composition changes completely: the marginal class went from a median depth of 1,354 m to the population's 126, and is now selected by a flat outlet, 4.5x gentler than the population. The coefficient is SOLVED per run rather than declared, because the published 161 had gone stale against the post-HYD-13 verdict and left 55 standing basins where Earth's density asks for 33.6 |
| GRAV-6 | Glacial erosion carries no gravity term, and Glen's law makes ice velocity go as `g^3` (2.23x here) | `notes/audits/orogen-gravity.md` | blocked on the downscaling pass, deliberately. The audit's recommendation was Option B, carry it as a declared gap: `iceFlow` is a heuristic accumulation rather than a Glen's-law velocity, so a rigorous g^3 on it is precision theatre. Decisive on top of that, a glacial valley is 1-5 km against a 15.19 km mesh cell, so the process is SUB-GRID and cannot be resolved here at any gravity. It belongs with the downscaling machinery, which has to persist sub-grid hypsometry anyway |
| SURF-1 | Implement the derived-surface classifier against the two-axis design | `pedology/notes/derived-surface-classes.md` | unblocked: `baseline_climatology` now names the baseline |
| SURF-2 | Re-run `brine_paths.py` weighted by discharge rather than catchment area | `pedology/notes/derived-surface-classes.md` | unblocked, same reason as SURF-1 |
| SURF-3 | Loess: needs the deposition field DUST-1 produces | `pedology/notes/derived-surface-classes.md` | UNBLOCKED, and no longer conditionally: DUST-1 is settled, DUST-6's gravity threshold has landed, and the field was re-run after HYD-13 changed the lake solution, so the `deposition_central` in `aeolian/analysis/dust_baseline.nc` is current rather than about to move |
| CLIM-2 | The first output bin of every orbit has corrupted WIND and HUMIDITY only -- ua 13.0x, spd 7.57x, va 0.72x, hus 0.71x, worsening downward from 1.02 at the model top. Every scalar is clean to better than 2%, so the flux bracket, soil, code 229, convergence assessments and the baseline run all stand | `notes/failure-modes.md` class 14 | open -- find the cause in the first output interval's accumulation. Two parts of this are now closed: the consumers are corrected (CLIM-3), and code 259 is settled, being exactly `sqrt(ua^2+va^2)` per sample in the snapshot product |
| DUST-11 | Settle the CATCHMENT half: run one prescribed-dust climate segment and measure what it does to precipitation and runoff. Runoff is the carve criterion's denominator and only 19% of land precipitation, so it amplifies; absorbing aerosol suppresses precipitation through a fast adjustment worth about 10% here, which maps to -10% to -31% on runoff depending on how much evaporation follows, and -10% on runoff is worth 144 basins | `notes/dust.md` | open, and it now GATES THE CARVE ON ITS OWN: DUST-10 measured the lake term at 24 basins in the UNDER-carve direction, so the catchment term at an estimated 144-235 the other way decides the sign. PRESCRIBED is enough -- the question is one iteration deep and emission need not respond -- so this is far smaller than DUST-3: the radiation must see a dust field and must have a longwave term, and nothing else. No emission scheme, no transport, no wet deposition, no size bins. The aerofile already exists. CHEAPEST ROUTE, and it is cheaper than it looks: for this question the transport is not needed either. We already have an offline AOD field per cell, so `numrhos` can be set from it directly and `aerocore` skipped altogether -- which also sidesteps the bottom-level sink, since nothing is being advected or accumulated. That reduces it to two changes: let the radiation see a prescribed field, and give it a longwave term. Neither needs `NAERO`, wet scavenging, or the emission law |
| DUST-3 | Put the emission scheme in the ExoPlaSim fork, keeping the source map outside it as boundary fields. DECIDED; DESIGNED in `aeolian/notes/in-model-dust.md`, which is detailed enough that landing it is mechanical | `aeolian/notes/in-model-dust.md`, `notes/dust.md` | open. "Replace one line" was wrong: six pieces in order -- the three upstream defects, replace the bottom-level sink with a deposition velocity, wet scavenging, boundary fields plus Kok 18, the longwave aerosol term, and size bins only if needed. The longwave is the dominant RISK (inside the radiation solver) and `aerocore` running serial on NROOT is the item most likely to be UNDERESTIMATED, since nothing the emission needs is gathered. Five or six patches to a compiled model, each needing a rebuild per CLAUDE.md rule 4. SEQUENCING unchanged: `WORKFLOW.md` A3, its own iteration, after iteration 2's baseline run |
| DUST-7 | There is NO wet deposition in ExoPlaSim's aerosol. `aerocore` takes no precipitation argument of any kind -- gravitational settling is the only removal, plus a bottom-level sink that is deliberate but not physical -- 99% per timestep, an implied deposition velocity of 52.7 cm/s against a real 0.1-2, and it does not scale with dt. See `aeolian/notes/in-model-dust.md` | `notes/dust.md` | open, part of DUST-3 and designed in `aeolian/notes/in-model-dust.md`. Mechanically the easiest of the physics additions: `dprl` and `dprc` exist, they are simply not handed over. `aeolian/README.md` says of our own chain that without the wet term the fine mode's lifetime is out by an order of magnitude, so porting emission in-model without adding scavenging would lose a first-order sink. Needs a loss term proportional to `dprl + dprc`, which is the same Sportisse (2007) route the offline chain already uses |
| DUST-8 | One aerosol, one radius, one density. `plasimmod.f90:99` fixes `parameter(NAERO = 1)`; `apart = 50e-9` m and `rhop = 1000` kg/m3 are photochemical haze against mineral dust at 0.1-20 um and 2650. The `do jc=1,NAERO` loops exist, but `apart`/`rhop` are scalars shared by every bin and `mmr2n` takes them as scalars | `notes/dust.md` | open, part of DUST-3 and designed in `aeolian/notes/in-model-dust.md`, which recommends the single effective radius first (0.98 um, already in the aerofile) with the offline chain kept for the size-resolved deposition loess and phosphorus need. Either promote size and density to per-bin arrays with per-bin optics, or pick one effective radius reproducing the mass extinction efficiency in `analysis/dust_optics.json` and declare what the single mode costs deposition |
| HYD-3 | Measure the overshoot: re-evaluate an already-carved set against the climate that carving produced, and report how many would no longer have carved | `WORKFLOW.md` section 4 | blocked -- it applies to a verdict taken on carved terrain, and a pre-carve base restarts the count at iteration 1 |
| CONS-1 | `check_consistency.py`'s biosphere check compares a sha256 of the raw `config/planet.yaml`, so an edited COMMENT is indistinguishable from an edited parameter and reports the generated inputs as stale. Fired today on a two-line comment change | `scripts/check_consistency.py` | open, small. This is the same defect `continue_exoplasim.py:config_drift` already fixed for resumes, and the fix is the same: compare parsed values, with an allowlist of keys that cannot reach the artifact. Low harm -- it fails loudly and the remedy it names is correct -- but it trains people to re-run generators to silence a hash rather than because anything moved |
| CLIM-7 | The baseline fails its own convergence criterion and was assessed as missing it by 0.0014 W/m2. Mean TOA over orbits 55-76 is -0.57 and over the clean 67-76 block -0.60, against `|mean TOA| < 0.5`. Find which window the 0.0014 was evaluated on and whether the assessment is picking a favourable one | `exoplasim/scripts/assess_convergence.py`, `exoplasim/notes/water-and-energy-closure.md` | open. Cheap to check and it bears on whether the baseline should be called quasi-equilibrated at all |
| CLIM-5 | Patch PlaSim's low-I/O output path, which writes a corrupt first record per model call. Runs now set `NLOWIO = 0` to avoid it at 25x the output volume, so this buys back disk rather than correctness | `exoplasim/notes/first-output-bin.md` | open -- do NOT write it on the obvious candidate. `naccuout` persists across runs through the restart while the accumulators do not (`plasim.f90:767`), but a counter error scales a field uniformly and this defect changes its vertical structure, 1.04 at the model top against 11.2 at the surface. The mechanism is unpinned |
| CLIM-6 | Give `denergy` an accumulator in `outmod.f90` beside `ashfl`, `alhfl` and the rest, so the 28 energy terms are time means like every other field on the stream rather than instantaneous values at the output step | `exoplasim/notes/water-and-energy-closure.md` | open, low priority -- NLOWIO = 0 already makes the terms usable, so this buys back the 25x output volume rather than correctness. Same standing caution as CLIM-5: it is a patch to a compiled model and every binary needs rebuilding |
| HYD-12 | Decide whether `TOLERANCE = 0.25` is still right now that Penman's ocean error is computed rather than hardcoded | `hydrography/scripts/carve_verdict.py`, `notes/audits/carve-criterion-terms.md` | done 2026-08-17 as NO CHANGE, with the reasoning. 0.25 is set by the terms that dominate -- assumed biosphere 3.7 to 7.1 K, dust 10 to 20%, sub-grid dry column up to 9.8% -- and Penman's own method error is the SMALLEST of them at 3.28%, so tying the tolerance to it would let the smallest term set the scale for the largest. The framing was the problem: `retain_margin` has never decided a basin and cannot, re-verified at 0 of 3,621 under the new mapping. The term that WAS deciding basins by fiat was the land-rate floor one line away in the same file, on 73% of the overflowing set; that is HYD-11 |
| PHYS-1 | Re-weight the Lacis & Hansen SHORTWAVE water vapour and CO2 absorptances for the K dwarf, as `o3uvw`/`o3visw` already do for ozone. They give absorptance as a fraction of total incident SOLAR flux, and this star puts 1.204x the Sun's share above 0.75 um where water vapour absorbs. Worth +6.5 to +9 W/m2 of atmospheric shortwave absorption and an equal reduction at the surface, against 21 W/m2 for the whole stellar sweep | `notes/audits/physics-review.md` | open, LARGEST physics finding. Same scheme, same argument and same patch as the ozone weights, left in the bigger term. Integrate the H2O band absorptances against k25v; `radmod.f90:311` already knows the solar reference share |
| PHYS-2 | Make the star's band-1 flux share canonical. Three values are in use: 0.3777 in `analysis/dust_optics.json` from `k25v.dat`, 0.3862 from `k25v_hr.dat`, and 0.4184 printed by the model itself. The model's is unexplained by either file and it weights the two-band snow and ice albedo, which is where a stellar-spectrum error previously cost a baseline re-run | `notes/audits/physics-review.md` | open. Negligible for dust (0.07% on the mass extinction efficiency), 0.013 on broadband snow albedo. Find why the model's integration differs, then have one value and let everything read it. NOTE for whoever takes it: `exoplasim/scripts/dust_forcing.py` HARDCODES the band shares as 0.3777/0.6223, and `analysis/dust_optics.json` carries 0.3777 as `stellar_flux_fraction_band1`. Both are the `k25v.dat` value, so whichever number wins moves the dust forcing as well as the snow albedo |
| CLIM-1 | Name the residual between the top-of-atmosphere and surface budgets. On ten clean NLOWIO = 0 orbits the gap is -0.4361 +/- 0.0737 W/m2 | `exoplasim/notes/water-and-energy-closure.md` | open, and much narrower. The 28-term decomposition is now CLOSED and is not the answer: radiative agrees to -0.023, sensible to +0.004, and the latent residual of +0.415 pairs exactly with the -0.4211 snowmelt booking, which is the fusion of snow released aloft and consumed at the surface. The whole budget closes to 0.02 once dissipation is counted. What remains is that the system reports a persistent -0.57 W/m2 TOA loss while surface temperature is FLAT, so the candidates are now `rst`/`rlut` themselves, which sum to `ntr` by construction and cannot be cross-checked, or an unnamed storage term in the slab or the sea ice |
| BIO-2 | Quote productivity with the `nfix_a` bracket 0.102-0.367 carried through rather than the central value alone; the span is 18.2% of NPP and it is the largest nitrogen lever | `biosphere/notes/productivity-prediction.md` | blocked on iteration 2's baseline run -- no LPJ-GUESS run exists on this build |
| GRID-1 | One utility owns the Orogen/ExoPlaSim grid convention, and nothing else computes a column | `lib/gridding.py`, `notes/audits/grid-convention-and-runoff.md`, `notes/audits/carve-criterion-terms.md` | done 2026-08-17 as PREVENTION rather than translation. `lib/gridding.py` now owns `column`, `column_fraction`, `row`, `cells`, `coupling_cells`, `require_same_rows` and the display rotation; `build_hydrography`, `basin_means`, `region_grid_cells`, `analyze_climatology.shifted`, `analyze_smoke` and `build_basemap` all go through it and none derives a column. `smoke_test.py` lints for longitude arithmetic outside that file, with only the two pure rasterisers in `maps/` exempt. Applying the invariant to every remaining site found TWO MORE live instances of the antipodal defect that survived HYD-13's sweep: `surface_water.py:per_basin_forcing` sampled every basin's lake precipitation and evaporation 65 columns out, putting 52.47% of sinks on model-ocean cells against 3.04%, and `maps/build_basemap.py:upsample` drew every climate layer on the basemap 180 degrees from its own terrain |
| HYD-14 | Bracket the seasonal rectification in catchment runoff | `notes/audits/grid-convention-and-runoff.md`, `notes/audits/carve-criterion-terms.md` | wontfix 2026-08-17: the premise is wrong and the denominator is already the conserved quantity. Over one annual cycle at steady state a land cell's storage returns to where it started, so `annual(P - E)` equals its runoff generation exactly; the negative bins are the store being drawn down, and the water that refills it did not run off. The model's own soil water says so cell by cell -- the ratio of its seasonal range to the discarded deficit has a MEDIAN OF 0.988, quartiles 0.95 to 1.07. So 266 mm/yr is not an upper bound, it is the wet season counted twice, and [161, 266] collapses. The clamp stays after aggregation, where the residual noise on a non-negative quantity has averaged down most. The rectification that DOES exist is in the lake rather than the catchment, by A2's ratchet argument, and is worth 8 of 1,752 sub-spill basins |
| HYD-11 | Quantify and correct the dry-column Penman overstatement | `notes/audits/missed-couplings.md`, `notes/audits/carve-criterion-terms.md` | done 2026-08-17, and it was two errors of very different size plus a third that was larger than both. FIXED: the level mixing. `es_a` at 2 m against `e_air` at the lowest model level is 1.85 K and 13.7% on `e_s`; reading all four terms at the level the transfer coefficient is derived over moves the ocean validation from 8.45% high to 3.28% low, which is the falsifiable test. FIXED: the floor `penman = max(penman, evap)`, which bound on 60.2% of land cells and put the aridity index at exactly -1.0 on 1,252 of 2,543 basins with runoff. It was not catching a Penman failure, it was catching the wet regime -- land here is 6.4x rougher than open water, so a smooth lake in a rough wet landscape really does evaporate less than the ground. BRACKETED, not applied: the sub-grid moistening, worth 1 to 35 basins rather than the 144 to 235 first estimated, because the level fix absorbs most of it and the sensitivity it was multiplied by was itself floor-inflated. Combined 1721 -> 1790 carves on the ratio form; the parts do not sum |
| HYD-16 | Rebuild `surface_water.nc` and the lake compositing in albedo 174-176 before the next baseline run, and re-render the basemap | `notes/audits/carve-criterion-terms.md` finding 8, `notes/audits/grid-convention-and-runoff.md` finding 1 | open. The CODE is fixed, the ARTIFACTS are not. `surface_water.nc` on disk was solved with every basin's lake precipitation and open-water evaporation taken from its antipode, and with the land-rate floor in place, so both the lake areas and the -0.018 they are worth on land-mean albedo are wrong. It is not regenerated here because regenerating it without re-running the climate would pair a corrected surface field with a climatology built from the uncorrected one. It belongs in the baseline re-run `grid-convention-and-runoff.md` already forces, before CYC-1. The rendered basemap is stale for the same reason and is cheap |
| HYD-17 | Decide whether the 1,062 basins whose catchments deliver no runoff are a real population or the length of the climatology window | `notes/audits/carve-criterion-terms.md` finding 4 | open, and cheap to settle. A land cell cannot have a negative annual `P - E` here: the mask is binary, routed runoff never re-enters the evaporating bucket, and the store is periodic at steady state. It nonetheless shows on 1,597 of 4,105 land cells with a mean of -3.1 mm/yr, which is the residual non-periodicity of a five-orbit window -- land-mean drift 8.6 mm per bin. Those cells concentrate at high latitude, which is where the dry catchments are and where the seasonal store is largest. A longer seasonal window costs orbits rather than physics and would say whether that population is 1,062 or several hundred fewer |
| BUDG-1 | Measure this world's hydrological sensitivity: one perturbation run reporting dP and dE per kelvin, giving a conversion from the error budget's kelvin into runoff. Runoff is 15.5% of land precipitation, so it amplifies dP by 6.5x and dE by 5.5x, and the carve verdict reads runoff while every budget item is priced in kelvin | `notes/audits/missed-couplings.md` | open. The single measurement that lets the existing budget rank anything against the carve, which is the one irreversible output |
| BUDG-2 | Re-express `analysis/error_budget.json` with a second currency alongside kelvin: effect on runoff and on basin count | `notes/audits/missed-couplings.md` | blocked on BUDG-1 for the conversion |
| BUDG-3 | Move the error budget's content into `scripts/error_budget.py` and give `analysis/error_budget.json` the same "generated, never edited" rule `world_state.json` has. The DUST-2 and DUST-6 corrections were written to the JSON and are absent from the repository; the file still says dust is "cooling everywhere" and that aerosols cannot be enabled, both of which later work contradicts | `notes/audits/missed-couplings.md` | open, and do this FIRST of the budget items, because until it is done any correction to the budget may evaporate the same way |
| BUDG-4 | Make the flux-to-kelvin conversion canonical. Three sensitivities are in use and they are not interchangeable: 150.2 K per unit flux ratio in `scripts/error_budget.py`, measured at T21 on the superseded `precarve-zoned-g1281` across 0.95-1.00, a bracket that no longer contains the 0.945 baseline; 196 and 209 on this build's own converged T42 points; and the stellar sweep's 33 K for 21 W/m2, which is 3.4x the local slope because it crosses the ice transition | `scripts/error_budget.py`, `notes/dust.md`, `WORKFLOW.md` section 5b | open. Found while refreshing the dust numbers: `notes/dust.md` used two of the three in adjacent paragraphs, and the budget's constant sets every kelvin in it, so the whole ranking is about 25% low if the T42 slope is right. Settle the DENOMINATOR with it: a top-of-atmosphere forcing is an absorbed-flux change and is being divided by incident insolation, which understates by a further 1/(1-alpha), about 1.36x. Same shape as PHYS-2 -- the answer is one value everything reads, not three defensible ones |
| BIO-4 | Record in `WORKFLOW.md` section 4 that the biosphere reaches the climate through albedo, roughness and forest fraction only, and not through transpiration: ExoPlaSim's land surface has no stomatal or LAI control and no rooting depth. `pedology/README.md` cites 100-150 mm of transpiration sensitivity from a bedrock vadose zone, against a runoff of the same order | `notes/audits/missed-couplings.md` | open, documentation only. Section 4 currently states the coupling in radiative terms alone and reads as though that were all of it |


## Closed

Moved to `archive/tasks.md`, with the reasoning that closed each one. Ids are
never reused, so look there when a commit cites an id this file does not have.
