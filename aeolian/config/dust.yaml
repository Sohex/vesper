schema_version: 1

# Calibration for the offline dust component. Every constant here is an Earth
# calibration or a declared assumption, each with its source and what it is
# worth, in the same way `pedology/config/pedogenesis.yaml` carries its own.
# Nothing in `scripts/` hardcodes a Vesper number.
#
# DECLARED BEFORE THE FIRST RUN, 2026-08-17. Values that could not be taken from
# a source carry a `bracket`, and the model reports the spread across it rather
# than the central value alone. Do not tune anything here to land on a number;
# `notes/dust.md` fixes a reopening test at land-mean AOD 0.10 and 1.5 W/m2, and
# tuning toward or away from it would make the test meaningless.

# --- emission: Kok et al. (2014) brittle fragmentation -----------------------
#
# Kok, Mahowald, Fratini, Gillies, Ishizuka, Leys, Mikami, Park, Park, Van Pelt,
# Zobeck (2014). An improved dust emission model - Part 1. Atmos. Chem. Phys.
# 14, 13023-13041. doi 10.5194/acp-14-13023-2014. Read; see references/INDEX.md.
#
# Equation 18a, the vertical dust flux:
#
#   Fd = Cd fbare fclay (rho_a (u*^2 - u*t^2) / u*st) (u*/u*t)^(Ca (u*st - u*st0)/u*st0)
#
# for u* > u*t, with Cd = Cd0 exp(-Ce (u*st - u*st0)/u*st0), equation 18b.
#
# Chosen over Marticorena-Bergametti because K14 derives the emitted size
# distribution from fragmentation physics rather than fitting it, so it carries
# one fewer unconstrained knob. On a synthetic planet with no aerosol optical
# depth observations to tune against, a globally tuned constant is worth nothing.
emission:
  # Fitted by K14 against a quality-controlled compilation of vertical dust flux
  # data sets; the +/- values are theirs and are carried as the bracket.
  cd0: 4.4e-5              # dimensionless, K14 section 3: (4.4 +/- 0.5)e-5
  cd0_bracket: [3.9e-5, 4.9e-5]
  ce: 2.0                  # K14: 2.0 +/- 0.3
  ce_bracket: [1.7, 2.3]
  c_alpha: 2.7             # K14: 2.7 +/- 1.0
  c_alpha_bracket: [1.7, 3.7]
  # Standardized threshold friction velocity of an optimally erodible soil, a
  # bed of 100 um loose sand. K14 section 2.3.
  u_star_st0_m_s: 0.16
  # Air density at sea level, the standardization reference. K14 equation 6
  # defines u*st = u*t sqrt(rho_a / rho_a0).
  rho_a0_kg_m3: 1.225
  # Standardized threshold for a typical erodible soil, K14's own stated value.
  # The bracket spans an optimally erodible bed at u*st0 to a crusted, resistant
  # surface. This is the single most influential constant in the scheme, because
  # the flux depends on u*st both directly and through the exponent.
  u_star_st_typical_m_s: 0.20
  u_star_st_typical_bracket: [0.16, 0.30]
  # Upper limit on the clay fraction the flux is linear in. K14 equation 3 takes
  # fclay as the mass fraction of the soil that fragments into dust, and the
  # sandblasting-efficiency relation it inherits from Marticorena and Bergametti
  # (1995), eta = 10^(13.4 fclay - 6), is stated for fclay below 0.2 and rises
  # without bound above it. Capping is the standard treatment and it is the
  # reason boundary field 1801 is written CLIPPED: applying the cap in the
  # generator keeps it outside the model, where the rest of the soil description
  # already lives.
  f_clay_max: 0.2

  # GRAVITY. Both thresholds above are Kok's, fitted on Earth, so Earth's
  # gravity is inside them. Kok's own standardization (equation 6) corrects for
  # air density and for nothing else, so on a 1.31 g planet they are too low and
  # the error is one-signed.
  #
  # The exponent is derived rather than guessed, and it is NOT sqrt(g).
  # Shao and Lu (2000) equation 22 gives
  #
  #     u*t^2 = f(Re*t) * (sigma_p g d + gamma / (rho d))
  #
  # a gravity term rising with grain size and a cohesion term falling with it.
  # The threshold has a minimum where the two are equal -- Kok et al. (2012)
  # section 2.1 says exactly that, "the interparticle forces are approximately
  # equal in magnitude to the gravitational force" at the 75-100 um minimum --
  # and setting d(u*t^2)/dd = 0 gives
  #
  #     d_opt      = sqrt(gamma / (rho_p g))
  #     u*t,min^2  = 2 f sqrt(rho_p g gamma) / rho      ->   u*t,min ~ g^(1/4)
  #
  # So at the optimum the scaling is the FOURTH root, and gamma cancels out of
  # the ratio entirely. That matters, because gamma is only known to a factor of
  # three: Shao and Lu give 1.65e-4 to 5.0e-4 N/m and Kok and Renno (2006) fit
  # 2.9e-4. Checked numerically across that whole range and the ratio is 1.0691
  # to four figures in every case.
  #
  # Using sqrt(g) instead would give 1.1429 and over-correct by 2.1x on the
  # correction. u*st0 is defined as an OPTIMALLY erodible bed, which is the
  # minimum, so the fourth root is the right exponent for it; u*st_typical sits
  # slightly off the optimum, where the true exponent is between 1/4 and 1/2, and
  # the same factor is applied to both because the K14 relations for Cd and alpha
  # depend on (u*st - u*st0)/u*st0, which is invariant under a common scaling.
  gravity_scaling_exponent: 0.25
  gravity_reference_m_s2: 9.80665

# --- threshold: soil moisture ------------------------------------------------
#
# Fecan, Marticorena, Bergametti (1999). Parametrization of the increase of the
# aeolian erosion threshold wind friction velocity due to soil moisture for arid
# and semi-arid areas. Ann. Geophys. 17, 149-157. doi 10.1007/s00585-999-0149-7.
# Read; equations 14 and 15.
#
#   u*tw / u*td = 1                                for w <= w'
#   u*tw / u*td = [1 + A (w - w')^b]^0.5           for w > w'
#   w' = 0.0014 (%clay)^2 + 0.17 (%clay)
#
# w is GRAVIMETRIC soil moisture in percent. Their fitted A and b' are 1.21 and
# 0.68, and they report the mean relative error on w' as about 12%.
moisture:
  a: 1.21
  b: 0.68
  clay_quadratic: 0.0014
  clay_linear: 0.17
  # w is GRAVIMETRIC and the artifacts carry a DEPTH of water, so these two
  # convert between them: w[%] = 100000 * water[m] / (soil_depth * bulk_density).
  # Both are declared rather than measured, and they are here rather than in a
  # script because the in-model scheme needs the same conversion. It reaches
  # ExoPlaSim as the single namelist constant `dustwcv`, derived from this pair
  # by aeolian/scripts/build_dust_source_fields.py, so there is one statement of
  # it and not three.
  #
  # 1500 kg/m3 is an ordinary mineral-soil bulk density; 0.5 m is the layer the
  # ExoPlaSim bucket is being read as. Emission is not strongly sensitive to
  # either except near the Fecan residual, where the threshold turns on.
  soil_depth_m: 0.5
  bulk_density_kg_m3: 1500.0

# --- threshold: drag partition ------------------------------------------------
#
# Marticorena, Bergametti (1995). Modeling the atmospheric dust cycle 1: design
# of a soil-derived dust emission scheme. JGR 100(D8), 16415-16430.
# doi 10.1029/95JD00690. Read; the drag partition of section 2.
#
#   feff = 1 - ln(z0/z0s) / ln(0.35 (X/z0s)^0.8),  z0, z0s, X in centimetres
#
# The 0.35 is Pendergrass and Arya's adiabatic value, which MB95 retain because
# it is the condition consistent with the logarithmic wind profile they assume;
# 0.8 is Elliott's exponent, confirmed by Glaser; X = 10 cm is the compromise
# they show is within 20% of a rigorous integration for feff above 0.2.
drag_partition:
  x_cm: 10.0
  a: 0.35
  b: 0.8
  # Smooth roughness of the erodible bed itself, taken as Dp/30 for a 100 um
  # particle. Declared rather than measured; the bracket spans a fine silt bed
  # to a coarse sand one.
  z0s_cm: 3.3e-4
  z0s_bracket_cm: [1.0e-4, 1.0e-3]
  # WHICH ROUGHNESS THE SCHEME IS FED, and it is not the obvious one.
  #
  # `orogen_T42_surf_0173.sra` is a GRID CELL effective roughness: a cover term
  # from this world's own lithology and land cover, combined with an orographic
  # form drag derived from this world's own subgrid slope through Wood and
  # Mason (1993) and Beljaars et al. (2004). Nothing in it is solved onto
  # another planet's land mean. `build_surface_roughness.py` carries the
  # derivation and its declared bracket; read the arm and the land mean out of
  # `roughness_<rung>_report.json` rather than assuming either.
  #
  # MB95 returns zero above a z0 of about 4.4 mm, and that field is orders
  # above it over nearly all of the source area, so feeding it
  # in shuts emission off almost everywhere. That is not a result, it is a
  # category error, and it is one at any land mean: MB95 partitions stress between
  # a bed and centimetre-scale roughness ELEMENTS standing on it, and a grid
  # cell's orographic variance is not a roughness element whatever its
  # magnitude. Dust models that use MB95 in a GCM carry a separate aeolian
  # roughness map for exactly this reason.
  #
  # So the scheme is driven by the aeolian roughness of the erodible surface
  # itself, and the grid-cell roughness is not used for the partition at all.
  # The consequence, which should be stated wherever a number from this
  # component is quoted: SHELTERING OF AN ERODIBLE PATCH BY THE TERRAIN AROUND
  # IT IS NOT REPRESENTED, and that biases emission high by an unknown amount.
  #
  # The bracket below is the real uncertainty and it is wide on purpose: a
  # wet-smoothed clay pan is near the bottom, a playa carrying pebbles, ripples
  # and small dunes is near the top. Emission goes as roughly u* cubed and u*
  # goes as 1/ln(z/z0), so this bracket is not a small correction.
  aeolian_z0_m: 1.0e-4
  # THE LOW END IS z0s ITSELF, and that is the physical floor rather than a
  # choice. `drag_efficiency` computes 1 - ln(z0/z0s)/ln(a (x/z0s)^b), which is
  # 1 at z0 = z0s: a bed whose only roughness is its own grains keeps all of its
  # stress, and there is no such thing as less sheltering than none. The function
  # clamps at `z0s * 1.0001` for exactly that reason.
  #
  # It was declared at 3.0e-6, BELOW the clamp, so that arm silently evaluated
  # the clamp instead and any value at or under 3.3e-6 gave the identical drag
  # partition. The bracket says it is over the sheltering, and at its low end the
  # sheltering was saturated rather than bracketed. Stating the floor is honest
  # about what that arm is: the no-sheltering LIMIT, not a surface anyone expects
  # to find. world-494.
  aeolian_z0_bracket_m: [3.3e-6, 1.0e-3]
  # PER ERODIBLE LITHOLOGY, each sourced, because the scalar above was the whole
  # of the answer: across its old bracket the emission moved by a factor of
  # 370,000 and nothing else in the calculation is close to that leverage
  # (world-c8m). The two classes this world's `source.erodible_weight` names are
  # not the same surface and the literature says so directly.
  #
  # Marticorena et al. (1997), on Saharan paleolakes: lakes fed by streams
  # "loaded with detritic sediments became clayey plains with SMOOTH SURFACES",
  # while "paleolakes, fed by superficial gypseous water, became EMBOSSED
  # TOPOGRAPHY, sensitive to weathering", and the present features of sebkhas
  # "vary, depending on their salt content and on the experienced periods of
  # drying and moistening". That distinction is between a CLAY PLAIN and a salt
  # pan, and it is why the two classes carry different roughnesses rather than
  # one scalar.
  #
  # IT DOES NOT ORDER THESE TWO CLASSES, and reading it as if it did is what put
  # `playa_clastic` an order of magnitude too smooth. Where the two surfaces
  # have both been measured aerodynamically they agree: MacKinnon's salt-crust
  # playa at 1.4e-4 m and Greeley's silty-clay Lunar Lake playa at 1.26e-4 and
  # 1.82e-4 m. What separates the classes here is not clay against salt but
  # extent: `evaporite` is the lowest quarter of a basin's relief and is a floor,
  # while `playa_clastic` is everything above it and is mostly basin margin. The
  # margin is the rougher surface, so this class comes out rougher than the salt
  # crust, which is the reverse of what the smooth-clay reading predicts.
  #
  # The values are aerodynamic roughness lengths of the ERODIBLE PATCH, which is
  # what `drag_efficiency` takes -- not the grid-cell roughness. Cells combine
  # them as an erodible-area-weighted GEOMETRIC mean, because the drag goes as
  # 1/ln(z/z0) and it is ln(z0) that averages.
  aeolian_z0_by_class_m:
    playa_clastic:
      z0: 3.9e-4
      bracket: [1.0e-4, 8.0e-4]
      # THE CLASS IS NOT A CLAY PLAIN AND THIS VALUE IS A MIXTURE, which is the
      # whole of why it is five times the salt crust below rather than a fifth
      # of it. `playa_clastic` is the residual of two geometric conditions in
      # Orogen -- inside a preserved closed basin, and not in its lowest quarter
      # of relief -- and Orogen's own name for it is "Playa mud / alluvial fan
      # fill". Measured on the mesh, half of it sits above 83 per cent of its
      # basin's relief and a third to a half lies on gradients a mud flat does
      # not have. So the surface property it needs is a mixture of a playa's and
      # a fan's, not a playa's.
      #
      # Greeley et al. (1997) Table 2 supplies both endmembers as aerodynamic
      # wind-profile determinations, the same quantity `evaporite` takes from
      # MacKinnon: Lunar Lake playa, silty-clay, 1.26e-4 and 1.82e-4 m; the
      # Death Valley alluvial fans, 6.96e-4 to 1.06e-2 m; the Gobabeb interdune
      # sand sheet and gravel lag, 4.0e-5 and 4.2e-4 m. Mixed geometrically over
      # the class's own landform bands this gives the central value, and the
      # bracket is the same mix with every endmember at its measurement set's
      # extremes, which is the convention `evaporite` already carries.
      #
      # WHAT IT REPLACED, and why the replacement is worth a factor of 13 on the
      # central value: a modelling convention at the smooth end, "a low surface
      # roughness on the order of 0.001 cm is used to describe active dust
      # sources", and Prigent's sand-desert boundary "z0 below ~0.02 cm" stepped
      # down one order at the rough end on the argument that a clay plain is
      # smoother than a sand sheet. A third to a half of this class is a gravel
      # apron, which is rougher than a sand sheet, so that step ran the wrong
      # way. The smoothest measured playa in the sources held, 1.26e-4 m, sits
      # ABOVE the whole of the bracket that argument produced.
      #
      # The derivation is `aeolian/scripts/playa_roughness_mix.py` and
      # `aeolian/analysis/playa_roughness_mix.json`; the criteria it was fixed
      # under are in `notes/audits/dust-intensity-levers.md` section 4. Read
      # from Greeley's own Table 2 and not from Prigent et al. (2005) Table 1,
      # which reproduces it and prints Golden Canyon NE 1989 a factor of 9 high.
      source: greeley1997 table 2, mixed over the landform bands in notes/audits/orogen-lithology.md
    evaporite:
      z0: 1.4e-4
      bracket: [5.3e-5, 3.9e-4]
      # MacKinnon et al. (2004) measured this surface directly. Their site 208
      # is a playa carrying `saltpush` -- "salt push-ups or ridges created by
      # expanding salt and clay crusts on playa surfaces", 2 cm high, 0.5 cm
      # wide, 5 cm apart -- and it is the strongest sheltering species there,
      # threshold friction velocity ratio 0.75 against 0.98 and 0.99 for the
      # sparse bushes beside it. The aerodynamically derived z0 at that site is
      # 0.014 cm with a range of 0.0053 to 0.039 cm over the measurement set,
      # which is this central value and this bracket unchanged.
      #
      # It agrees with the only other aerodynamically measured playa in the
      # sources held, Greeley's silty-clay Lunar Lake at 1.26e-4 and 1.82e-4 m,
      # which is what licenses using one paper for this class and another for
      # the class above. It comes out SMOOTHER than `playa_clastic` because that
      # class is basin margin and this one is basin floor, not because a salt
      # crust is smooth: Prigent et al. (2005) record the opposite for the
      # surface itself, the salt desert of Dasht-e Kevir "does not appear as a
      # smooth surface on the ERS map, maybe because of specific properties of
      # salty soils."
      source: mackinnon2004 tables 1 and 2, site 208
  # THE SPREAD OF z0 WITHIN ONE CLASS, which the values above collapse to a
  # point. A playa has a smooth interior and rougher margins, and tabulating one
  # value per lithology throws that away. Menut et al. (2013) name the
  # consequence: "The LISA dust fluxes are, on average, higher than the other
  # model configurations. This is mainly due to the distribution of z0 values:
  # being TABULATED, values are more discrete and thus less variable and
  # realistic than those provided by ERS from satellite data."
  #
  # MEASURED, from the one source in this repository that gives several in-situ
  # z0 values for the SAME area. Greeley et al. (1997) Table 2 carries three
  # such groups: ten values over Death Valley, two at Gobabeb in the Namib, and
  # two on Lunar Lake playa in central Nevada. Pooling the within-group variance
  # of ln z0 over all three gives the central value; the smallest and largest
  # single-group estimates give the bracket, and both of those rest on two
  # points, which is why the pooled figure with eleven degrees of freedom is the
  # one to quote.
  #
  # TAKEN FROM GREELEY AND NOT FROM PRIGENT ET AL. (2005) TABLE 1, which
  # reproduces those rows and prints Golden Canyon NE 1989 as 1.00e-0 cm against
  # Greeley's 0.001100 m, a factor of about 9. That row sits in the Death Valley
  # group, which carries nine of the eleven degrees of freedom, and pooling over
  # the reproduction inflated the spread. `playa_roughness_mix.py` derives this
  # from the same corrected table the class roughnesses above come from.
  #
  # IT IS AN UPPER BOUND ON THE WITHIN-LITHOLOGY SPREAD, deliberately. A Death
  # Valley box holds playa, alluvial fan, dune and lava, so its spread is across
  # surface types as well as within one; nothing in the table separates them.
  # Reading it as within-lithology therefore overstates the spread, and the
  # tabulation bias derived from it overstates the correction in the same
  # direction.
  #
  # DECLARED AS A REPORTED BIAS, NOT FOLDED INTO THE ARMS, and the reason is
  # double counting. Each class's `bracket` above is the same kind of range: for
  # `evaporite` it is explicitly MacKinnon's spread over one measurement set,
  # which IS a within-class spatial spread. Using it once as a level bracket and
  # again as a within-class distribution would count it twice. So the arms keep
  # the bracket as a level uncertainty and
  # `aeolian/scripts/dust_intensity_levers.py` prices what the collapse costs.
  within_class_z0:
    sigma_g: 2.523
    sigma_g_bracket: [1.297, 5.273]
    source: greeley1997 table 2, the three repeat groups; aeolian/scripts/playa_roughness_mix.py
# --- the subgrid wind distribution -------------------------------------------
#
# NOT OPTIONAL, and the reason is arithmetic rather than taste. Emission goes as
# roughly u* cubed above a threshold, so a gridbox-mean wind emits almost
# nothing while the same mean with realistic variance emits a great deal. The
# approach is Cakmur, Miller, Torres (2004), who integrate emission over a
# Weibull distribution of subgrid wind speeds.
#
# The shape parameter is DECLARED, not taken from a source, and it is the least
# grounded constant in this file. On this world it also has to absorb sub-bin
# temporal variance as well as subgrid spatial variance, because the climatology
# is a 12-bin mean, which argues for a broader distribution than a purely
# spatial one. k = 2 is the Rayleigh case.
# WHAT THE FITTED SHAPE CANNOT CONTAIN. The shape is fitted to high-cadence
# near-surface wind from a run, so it is the tail THE MODEL PRODUCES. A
# nocturnal-inversion low-level jet -- the Bodele mechanism, a sharp nocturnal
# inversion decoupling the surface and the jet above it mixing down after
# sunrise -- lives in the lowest few hundred metres, and the model's lowest full
# level sits near a hundred metres with the next near five hundred, so the
# inversion and the jet core fall inside one layer. Sampling the model's wind
# more often cannot recover a structure the model has no levels to form. The
# consequence is ONE-SIGNED and it is a floor, not a bracket: basin emission is
# understated wherever inversion jets would dominate it. Declared, not
# parameterised, because a parameterisation fitted to nothing is precision
# theatre; DUST-16, and notes/external-review-abl-obliquity-dust-carbon.md
# point 1 carries the argument.
subgrid_wind:
  weibull_shape: 2.0
  weibull_shape_bracket: [1.5, 3.0]
  # Integration is over this many quadrature points in the Weibull tail. Above
  # the threshold the integrand is steep, so the count matters; declared, not
  # convergence-checked at build time.
  quadrature_points: 96

# --- what can emit at all ----------------------------------------------------
#
# THE POINT OF DOING THIS OUTSIDE THE GCM. ExoPlaSim's own scheme is
# `fcoeff * land_mask`, which emits as much from forest as from salt pan.
#
# `substrate_class` is consolidated lithology PLUS closed-basin fill, so on this
# world the only unconsolidated surface material the export carries is the fill.
# Bedrock does not emit: it has no loose fines until weathering makes regolith,
# and regolith that supports vegetation is not a dust source either.
#
# The erodible weights are per rock class, relative to loose fine clastics at
# 1.0. Only the two barren classes are given any weight, matching
# `config/planet.yaml`'s `barren_rock_classes`, which is the project's declared
# position on what cannot carry vegetation.
source:
  erodible_weight:
    # Playa mud and alluvial fan fill: loose, fine, and the direct analogue of
    # the Bodele, which supplies a large share of Earth's dust from a single dry
    # lake bed. This is the reference class at 1.0 by definition.
    playa_clastic: 1.0
    # Salt crust. A cemented evaporite crust is NOT a silicate soil and resists
    # deflation, which is why the Bodele is efficient as diatomite rather than
    # as a salt pan. Kok's brittle fragmentation is calibrated on silicate soils
    # and does not describe halite cement, so this weight is a declared
    # suppression rather than a measured efficiency. It is the difference
    # between a dust world and a merely dusty one, so the bracket is wide.
    evaporite: 0.10
  erodible_weight_bracket:
    evaporite: [0.0, 0.30]
  # DESERT PAVEMENT, and it enters with the opposite sign to the one first
  # assumed. Wells, Taylor, Dohrenwend et al. (1995) dated pavement clasts with
  # cosmogenic 3He and found they have been at the surface throughout -- "stone
  # pavements are born at the surface" -- rejecting deflation lag outright. A
  # pavement is a clast mosaic over an accretionary Av horizon: dust falls
  # THROUGH it and is stored beneath, so the surface is a sink and its cover is
  # non-erodible.
  #
  # DECLARED, not measured. The paper establishes the mechanism and gives no
  # areal clast cover, so what a real mosaic leaves exposed between clasts is
  # not a number this project has. 0.0 is the paper's mechanism taken at its
  # word; the bracket runs to 0.25 because a mosaic is not a lid and an
  # interstice is erodible if the wind reaches it. Report the spread rather than
  # tuning inside it, as the evaporite weight above does.
  pavement_residual_erodibility: 0.0
  pavement_residual_erodibility_bracket: [0.0, 0.25]
  # Fraction of a cell's land that is bare erodible soil is further reduced by
  # standing water, snow cover, and vegetation. Lakes and snow are taken from
  # artifacts; vegetation is the assumption below.
  snow_suppression_depth_m: 0.01   # snow deeper than this shuts a cell off
  # THE ASSUMPTION THAT MOST LIMITS THIS PRODUCT. No vegetation model has run on
  # this build, so cover is taken from the project's existing declared position:
  # everything that is not a barren class carries a canopy and does not emit.
  # That is generous, and knowingly so: median land runoff on this build is
  # 3.8 mm per Earth year with two thirds of land under 50, which is not a
  # canopy. The `arid_bare_ground` variant below brackets it by letting dry
  # cells emit regardless of lithology, and the model reports both.
  arid_bare_ground:
    enabled_as_bracket: true
    # A cell drier than this is treated as bare regardless of its rock class.
    # Chosen as the conventional desert boundary rather than fitted.
    precipitation_mm_per_earth_year: 250.0
    # Weight given to bare bedrock regolith when it does qualify. Lower than
    # playa fill because bedrock regolith is coarser and partly armoured.
    weight: 0.30

# --- transport and removal ---------------------------------------------------
transport:
  # Dust is carried as a well-mixed column of this scale height, advected by the
  # mass-weighted wind of the layers below `steering_sigma`. A single steering
  # layer is a simplification; it is declared because the alternative is a
  # three-dimensional tracer solve, which is what the GCM would be for.
  dust_scale_height_m: 3000.0
  dust_scale_height_bracket_m: [2000.0, 5000.0]
  steering_sigma: 0.5
  # Steady state per time bin, reached by explicit stepping. Convergence is
  # checked rather than assumed.
  # The explicit step is CFL-limited by the convergence of the longitude grid at
  # the poles, and the finest bin relaxes on a timescale of years where nothing
  # rains. At 4000 iterations the solver reached 27.6 days against a 1351-day
  # relaxation time and reported not-converged, correctly.
  max_iterations: 40000
  convergence_fraction: 1.0e-4
  # Floor on cos(latitude) in the grid spacing, purely numerical. Without it the
  # polar rows set a 14 km cell width and a 597 s step for the whole globe. Dust
  # sources are not polar, so the distortion is where it does not matter, but it
  # IS a distortion of polar transport and it is declared rather than hidden.
  polar_coslat_floor: 0.2
  # Horizontal diffusion, as a numerical regulariser for the upwind advection
  # rather than as physics. Reported so it is not mistaken for a transport term.
  diffusion_m2_s: 50000.0

removal:
  # Gravitational settling is Stokes with the Cunningham slip correction, and it
  # uses THIS WORLD's gravity from config/planet.yaml, not Earth's. At 12.81
  # m/s2 settling is 1.31x faster than Earth for the same particle, so dust
  # lifetime here is shorter than terrestrial intuition suggests.
  particle_density_kg_m3: 2600.0     # matches analysis/dust_optics.json
  # Below-cloud scavenging, Lambda = A p^B with p the precipitation rate in
  # mm/h. Sportisse (2007), A review of parameterizations for modelling dry
  # deposition and scavenging of radionuclides, Atmos. Environ. 41, 2683-2698,
  # Table 12: for particle diameters near 2 um, A = 1.72e-6 and B = 0.61.
  #
  # This is the least certain term in the removal budget and it matters most:
  # without wet scavenging the fine mode's lifetime is out by an order of
  # magnitude. The bracket spans that table's own size dependence, from
  # 1.72e-6 at 2.16 um to 3.49e-6 at 1.66 um, and up to the NAME model's
  # 8.05e-5 for below-cloud snow scavenging.
  scavenging_a: 1.72e-6
  scavenging_b: 0.61
  scavenging_a_bracket: [1.72e-6, 8.05e-5]

# --- emitted size distribution -----------------------------------------------
#
# Kok (2011). A scaling theory for the size distribution of emitted dust
# aerosols suggests climate models underestimate the size of the global dust
# cycle. PNAS 108(3), 1016-1021. doi 10.1073/pnas.1014798108. Read; equation 6.
#
#   dV/dlnD = (D/cV) [1 + erf(ln(D/Ds) / (sqrt(2) ln(sigma_s)))] exp(-(D/lambda)^3)
#
# Constants are Kok's fitted values with his stated uncertainties.
size_distribution:
  median_diameter_um: 3.4          # Ds, 3.4 +/- 1.9
  geometric_sigma: 3.0             # sigma_s, 3.0 +/- 0.4
  crack_length_um: 12.0            # lambda, 12 +/- 1
  # Bin edges in micrometres. Four bins with independent settling and no
  # inter-bin microphysics, which is right for mineral dust: it neither
  # coagulates nor grows appreciably, so size resolution buys differential
  # settling and nothing else.
  bin_edges_um: [0.1, 1.0, 2.5, 5.0, 20.0]

# --- optics ------------------------------------------------------------------
#
# NOT recomputed here. `analysis/dust_optics.json` carries two-band mass
# extinction efficiency, single-scattering albedo and asymmetry parameter
# integrated against this star's spectrum, from a Mie code validated against
# Bohren-Huffman and OPAC. Optical depth is column mass times mass extinction
# efficiency.
#
# WHICH REFRACTIVE INDICES THIS WORLD'S DUST HAS IS DECLARED HERE, ONCE, AND
# EVERY CONSUMER READS IT FROM HERE. That is the point of the block. Until
# DUST-12 the offline forcing computed everything from OPAC while the aerofile
# the model reads was built from the measured datasets, so the two priced the
# same dust at absorption optical depths differing by a factor of 2.5. Neither
# was wrong; there was no declared answer for them to share. The consumers are
# `aeolian/scripts/build_dust.py`, `exoplasim/scripts/dust_aerofile.py`,
# `exoplasim/scripts/build_surface_dust.py` and
# `exoplasim/scripts/dust_forcing.py`, and all four resolve these names through
# `exoplasim/scripts/dust_indices.py`. The names are the `indices` labels in
# `analysis/dust_optics.json`, which is also where the numbers are.
#
# THE CHOICE IS THE MEASURED DATASETS IN THE SHORTWAVE, and it is a choice
# between two defensible datasets rather than a defect in either. Three
# arguments, in the order they should be weighed:
#
# 1. MINERALOGY. OPAC's mineral component carries its shortwave absorption in an
#    assumed hematite content, and is 1.0 to 2.1x more absorbing than the
#    measurements (`notes/dust.md`). Di Biagio et al. (2019) measured 19 natural
#    soils and Rocha-Lima et al. (2018) measured Saharan dust in the field.
#    This world's source is neither: it is closed-basin fill, `playa_clastic` at
#    weight 1.0 and `evaporite` crust at 0.1 above, and evaporite-bearing playa
#    dust is LESS absorbing than the Saharan silicate the measurements were made
#    on, not more. `analysis/dust_optics.json` already carries that as a caveat.
#    Both datasets are an extrapolation to this world and the measured one
#    extrapolates from the side this world's mineralogy is on.
#
# 2. INTERNAL CONSISTENCY. `dust_optics.py` integrates over the Balkanski et al.
#    (2007) source size distribution, and Balkanski is the paper that reevaluated
#    mineral dust forcing on the grounds that OPAC's indices are too absorbing.
#    Taking Balkanski's size distribution with OPAC's k is half of each of two
#    papers that disagree with one another.
#
# 3. THE DIRECTION OF ERROR FOR THE CARVE RUNS THE OTHER WAY, and that is why
#    the bracket below is kept rather than deleted. Absorption is what suppresses
#    precipitation through the fast adjustment, and precipitation sets the runoff
#    the carve criterion divides by, so choosing the less absorbing dataset
#    understates the suppression and therefore understates how much a dust-free
#    carve list OVER-carves. Over-carving is the irreversible direction. That is
#    an argument for PRICING the absorbing end, not for adopting it: adopting a
#    dataset believed to be too absorbing because the resulting verdict is safer
#    would be choosing an input for its outcome, which is what "physics is not a
#    knob" forbids. The bound is carried in `indices_bracket` and priced by
#    `dust_forcing.py --indices bracket`.
#
# THE LONGWAVE IS NOT THE SAME KIND OF CHOICE. The measured shortwave datasets
# stop at 0.95 and 2.45 um, so OPAC is the only dataset in this repository that
# reaches the thermal infrared at all, and its longwave basis is Volz (1973)
# Saharan dust. The measured alternatives are known and rejected for stated
# reasons rather than unexamined: Di Biagio et al. (2017) pins its k constant
# below 6 um by the authors' own statement, and Di Biagio et al. (2014) retrieves
# k over 2.5-25 um by the pellet method. Both are in `references/INDEX.md`.
#
# The consequence is a mixture and it is stated rather than hidden: the
# shortwave sits at the measured end and the thermal infrared at the absorbing
# one, which biases the NET forcing warm and the surface shortwave reduction
# small. Both
# push the lake limb of the carve toward fewer basins overflowing.
optics:
  source: analysis/dust_optics.json
  # The declared choice. `longwave` is read by anything integrating past 4 um:
  # `dust_forcing.py`'s thermal term and `build_surface_dust.py`'s DUSTQLW.
  indices:
    band1: Di Biagio 2019 measured
    band2: Rocha-Lima Algeria fine
    longwave: OPAC
  # The absorbing end, as a bound rather than an alternative. Priced offline by
  # `dust_forcing.py --indices bracket`. Running it IN the model would mean
  # regenerating the aerofile and the boundary field against it and paying for a
  # second climate run, which is not wired because it is not worth a run.
  indices_bracket:
    band1: OPAC
    band2: OPAC
    longwave: OPAC
