# Closed tasks

Every task that has been finished or abandoned, with the reasoning that closed
it. Moved out of `TASKS.md` so that file shows only what is left, and kept
rather than deleted for two reasons.

**IDs are never reused.** A closed id has to stay findable, because commits,
notes and audits cite it. Search here first when an id in a commit message does
not appear in `TASKS.md`.

**The reasoning is the point.** A `wontfix` records why something that looked
like work turned out not to be, and several here reverse an earlier conclusion.
Deleting them would lose the argument and invite the same task being opened
again.

Same conventions as `TASKS.md`: one line per task, every task cites the document
that justified it, and the closing note says what was actually done rather than
that it was done.

| id | task | source | closed |
| --- | --- | --- | --- |
| HYD-7 | Test the SPILL CAP, the half of the lake solver this world actually exercises | `hydrography/notes/lake-solver-validation.md` | done -- narrowed to the question the cap turns on, what a 15 km representation does to measured storage, and answered on Copernicus DEM 90 m for two real endorheic basins in different tectonic settings. It is one-signed: mesh scale understates storage by 13% at 400 m of depth and 30-50% in the shallow range, and on a broad pan misses shallow flooding entirely, the Qaidam showing no flooded area at all 25 m above its floor. Understated capacity and understated area_at_spill both push toward spilling, so the carve verdict is biased toward over-carving, and Vesper's median mean depth at spill of 46.5 m sits in the band where the deficit is worst. Not a bug and not a correction: this terrain is generated at mesh scale and never had sub-grid depressions, so it is a one-signed bias for the error budget. The shape check passes separately, the solved lakes scaling as V ~ A^1.26 against Earth's 1.17-1.21 |
| HYD-4 | Validate the lake equilibrium solver against something. It is the least-checked product in the pipeline | `hydrography/notes/lake-solver-validation.md` | done, and it FAILED the criterion registered before the data was fetched: median absolute log10 of predicted over observed lake area 0.622 against a pass at 0.30, over 145 Earth terminal lakes from HydroLAKES, HydroBASINS, GLEV and NASA POWER. The failure is structured rather than noisy, correlating with runoff at +0.760, and two-sided: the wet quartile over-predicts fivefold because the relation has no spill cap, which `lake_balance.solve` does have, and the dry quartile under-predicts eightfold on groundwater and on Earth's arid lakes being relicts rather than equilibria -- which the design registered as a risk in advance. So the solver is not certified, the equilibrium RELATION is bounded at about a factor of two in the evaporation-limited middle, and the half this world actually exercises is untested: 1,737 of 2,465 Vesper basins sit at their spill. That half is HYD-7 |
| LITH-23 | Ground the PRIMARY CLAY fractions in `pedogenesis.yaml`, which set the texture of freshly produced regolith and are what actually controls the felsic-mafic contrast | `pedology/config/pedogenesis.yaml` | done, and not the way the task expected. Three sources fetched and none carries a per-rock table, because there is no such quantity: Bockheim (1980) puts parent-material clay at under 2% to 38% with the spread set by whether the material is till, alluvium or in-situ residuum. So the per-rock COLUMN was the error, not its values. Crystalline classes now share one primary clay of 0.08, which beats the old column on every metric -- bias +0.004 against +0.062, RMSE 0.084 against 0.104, divergence 0.152 against 0.231 where observed is 0.179, correlation unchanged -- and is insensitive across 0.05 to 0.10, which is what makes it structural rather than a fit. The granite-basalt divergence survives and is now carried entirely by inert quartz, which IS a rock property; the old column double-counted by giving basalt both more clay and less quartz. Sedimentary classes untouched, and no sedimentary site is in the validation set |
| CLIM-4 | `ua` and `va` on SIGMA levels are u*cos(phi) and v*cos(phi) rather than physical winds, so every consumer of those components understates by cos(phi) | `notes/failure-modes.md` class 15 | wontfix, REFUTED by measurement, and kept rather than deleted so the same false lead is not rediscovered in the same source file. The claim came from `RevCosPhi` in burn7.cpp's pressure-level path plus a ratio that grew with latitude. Pointwise on the raw artifact `MOST_SNAP.00086` the largest difference between `spd` and sqrt(ua^2+va^2) anywhere in the field is 1.9e-06 m/s, and the ratio is 1.0000 at 82 degrees where cos(phi) is 0.134 -- where the claim demanded 7.5. The real cause of the 1.5x is that `build_climatology.py` averages `ua` and `va` as vectors and `spd` as a scalar across its five-orbit window; the raw per-orbit snapshots show no discrepancy at all. The cos(phi) division applied to `aeolian/scripts/build_dust.py` on the strength of this has been reverted |
| DUST-4 | Decide whether a 12-bin climatology can drive dust at all, given that mean friction velocity sits below threshold so emission is entirely a tail property | `aeolian/README.md` | wontfix, withdrawn by its own author. The argument was raised while the emission chain was 1700x out, and a tail-dominated process reading far too HIGH is evidence the machinery is wrong rather than the driver. It was: one corrupted time bin in the climatology was 100.00% of the emission. The tail question is real but it is a measurement rather than a concession, and it is DUST-5 |
| LITH-22 | Ground the whole-rock quartz fractions in `pedogenesis.yaml` against a mineralogical source rather than QAPF read off a classification | `pedology/config/pedogenesis.yaml` | wontfix -- the premise was wrong and measuring it was the cheapest way to find out. Sweeping felsic quartz against the sixteen validation sites makes the divergence WORSE, 0.232 at the configured 0.28 rising to 0.296 at 0.55, because inert quartz leaves less material to convert; closing the gap that way needs granite below any real granite's modal quartz. The QAPF correction had already taken what mineralogy had to give. The contrast is controlled by the primary clay fractions, which describe fresh regolith rather than rock, so the work is LITH-23 against a different literature |
| HYD-1 | Route the zero-catchment-runoff basins whose lake surface has P > E through the marginal band, rather than the blanket preserve the guard in `export_carve_list.py` gave them | `hydrography/notes/retain-fraction.md` | done -- and no guard was needed in the end. The overflow test is now the discharge `Q = runoff*(catchment - area_at_spill) - (E - P)*area_at_spill`, which with no catchment runoff reduces to `(P - E)*area_at_spill`, so a lake surface that gains water carves its own basin by construction. Verified equivalent to the ratio test wherever runoff is positive, on 200,000 synthetic basins. How many such basins there really are is HYD-6 |
| HYD-2 | Make the retain fraction a function of overflow discharge rather than of distance from the evaporation threshold | `hydrography/notes/retain-fraction.md` | done -- `retain_incision = 1 - (Q/Q_full)**0.5` from stream power, with `Q_full` declared at 1 m3/s and recorded in the sidecar, combined with the old margin by taking the larger, since either is a reason to leave a rim standing. No absolute time-to-cut is attempted: K spans five orders of magnitude by lithology, which is HYD-5 |
| HYD-5 | Put the sill's own rock into the retain mapping: `basins.nc` carries `spill_region` and the export carries erodibility per region, so the K in `K Q^m S^n` was available and unused | `hydrography/notes/retain-fraction.md` | done -- `Q_full` is now divided by the geometric mean of the erodibility either side of each saddle, resolving for all 3,621 basins with the terrain hash checked first. The task as written cited Stock and Montgomery's five orders of magnitude as the motivation, and applying that would have been the error: Zondervan (2020) measures the contrast a channel network actually expresses at about 4x, because channels adjust width and slope. The export's field spans 3.85x across these sills, which is the expressed number and the one a landscape model wants |
| CONV-1 | Enforce cross-component pointing rather than assuming it: a climatology carried no build identity, so no consumer could check the world it described | `lib/provenance.py` | done -- build_climatology stamps source_build, run id, geography, flux and spectrum; lib/provenance.py is the one check; wired into build_soil, weathering_fluxes, thermostat_efficiency, build_lpj_driver and run_lpj_guess; check_consistency verifies the named baseline once |
| CONV-2 | LPJ-GUESS run ids were derived names, the same collision ExoPlaSim had | `biosphere/scripts/run_lpj_guess.py` | done -- UUIDs, with what the name encoded moved to a `physical` block in the run manifest |
| CONV-3 | `convert_orogen.py` was marked superseded while four live scripts imported `write_sra` from it | `exoplasim/scripts/sra.py` | done -- write_sra extracted to its own module, importers repointed, convert_orogen deleted |
| CONV-4 | hydrography's .gitignore rules were flat paths against per-build output, so regions.nc and surface_water.nc were tracked under every build despite being meant to be excluded | `.gitignore` | done -- per-build globs, verified with check-ignore that the two rasters are excluded and basins.nc still tracked |
| VOLC-1 | Re-examine the `melange` -> Meybeck `misc_metamorphic` mapping. It was chosen when melange was 0.00% of land; it is now 6.1% and supplies 46% of this planet's silicate CO2 drawdown | `pedology/scripts/weathering_fluxes.py` | done -- remapped to `shale`, and NEITHER of the two options first considered was right. misc_metamorphic is marble by its own chemistry (Ca+Mg 1740 vs HCO3 1730, 2.3% of Earth outcrop); gneiss is felsic crystalline basement, which a forearc is not. Orogen assigns melange to the WHOLE forearc province -- prism, forearc basin, serpentinite -- which is greywacke and argillite by volume, i.e. shale. Also fixed a second error it exposed: carbonate-bearing classes were entering the silicate CO2 total at full weight because only `sedimentary_carbonate_rocks` was tested by name. Implied outgassing 1.54x -> 1.07x Earth. Serpentinite is knowingly under-represented; at a generous 10% of melange it moves the silicate CO2 total by -0.5%, and it biases CO2 and Ca HIGH rather than low, Mg and silica low, because Meybeck's peridotite is LOWER in bicarbonate than shale |
| VOLC-4 | Decide whether CO2 stays prescribed at 450 ppm. Nothing in the project solves the carbonate-silicate balance and `outgassing` appears nowhere in it | `pedology/scripts/weathering_fluxes.py` | done -- STAYS prescribed, and is now checked rather than merely assumed. Closing the loop would replace a prescribed CO2 with a prescribed outgassing rate, no better constrained, at the cost of a weathering law in pCO2 and several coupled climate integrations: the free parameter moves, it does not go. Instead `weathering_fluxes.py` reports the requirement against a mass-scaling supply bound, and the decision is recorded with its caveats above the atmosphere block in `config/planet.yaml`. Re-run the check after the baseline re-run |
| VOLC-6 | Model andisols: volcanism as a process rather than a rock class, since andic material fixes phosphorus rather than supplying it | `references/INDEX.md` | done -- two gates, neither a free parameter. Ongoing ejecta (the arc alone is known active, being placed as a band about the volcanic front) and leaching at Dahlgren's ~1500 mm allophane/halloysite boundary. Emits `andic` and `pfixation` columns for the C-N-P fork, and blends bulk density and plant-available water. Most of this world's volcanic terrain turns out to be halloysitic, not andisol |
| VOLC-7 | Compute CO2 consumption and dissolved silica per lithology, and split them exorheic against endorheic | `pedology/scripts/weathering_fluxes.py` | done -- `weathering_fluxes.py`. Silicate CO2 kept separate from carbonate-derived, since conflating them roughly doubles the total and makes it incomparable with any published figure. Resolves the silica-source question that `derived-surface-classes.md` had left open |
| GRAV-5 | Bracket the slope exponent | `notes/audits/orogen-gravity.md` | done -- needs NO build: gravity is a post-hoc scalar, so n=2 is the existing export rescaled by 1.1427. Land mean 0.407 -> 0.465 km, peak 4.593 -> 5.249 km, land fraction and basin identity unchanged by construction. Worth ~0.4 K by lapse rate |
| GRAV-4 | State `n = 1` explicitly at the erosion call site; the whole gravity correctness argument rests on it and nothing says so | `notes/audits/orogen-gravity.md` | done -- stated in `vendor/orogen/js/terrain-post.js`, including that n=1 is baked into the Braun-Willett closed form rather than being a parameter, and that the post-hoc 1/g scaling depends on it |
| LITH-4 | Fetch bare-rock and evaporite albedo literature | `notes/audits/orogen-lithology.md` | done -- 16 sources fetched and indexed, every DOI confirmed against Crossref before fetching |
| LITH-10 | Pin down what `pelagic` means | `notes/audits/orogen-lithology.md` | done -- the class is `Pelagic ooze / abyssal clay`, assigned in the ocean domain with thickness growing by seafloor age. Unconsolidated, so 3.00 is correct beside Moosdorf's 3.2 |
| LITH-8 | `carbonate` erodibility 1.30 -> 0.45 | `notes/audits/orogen-lithology.md` | done -- applied |
| LITH-9 | `schist` erodibility 1.10 -> 0.45 | `notes/audits/orogen-lithology.md` | done -- applied |
| LITH-11 | `evaporite` density 2.2 -> 2.1 | `notes/audits/orogen-lithology.md` | done -- applied |
| REF-3 | Zhuang et al. (2023) solid-rock vs powder reflectance | `references/INDEX.md` | done -- SUPERSEDED. Paragas 2025, via the POSEIDON surface albedo database, measures the same rocks as slab, crushed and powder, which is the same conversion. Median powder/slab 2.74 |
| LITH-2 | Basin-fill albedo: do NOT split evaporite by mineralogy with gypsum brighter -- band-weighted under K2.5V gyps | `notes/audits/orogen-lithology.md` | done -- decided NOT to split evaporite by mineralogy. Within-mineral spread (halite 0.311-0.877) exceeds the between-mineral difference, and surface state dominates both |
| LITH-3 | Ground the 60 uncited rock-class constants | `notes/audits/orogen-lithology.md` | done -- erodibility against Moosdorf/Stock/Zondervan, density against Daly 1966, albedo against Paragas slabs, Longhi rock-slab classes and Post's soils. All three grounded |
| LITH-7 | Narrow the erodibility spread to ~4x via `--lithology-strength 0.682`. MEASURED: combined with LITH-8/9 the ma | `notes/audits/orogen-lithology.md` | done -- applied, --lithology-strength 0.682 in build precarve-zoned-g1281-erod4x |
| LITH-12 | Rebuild the albedo table by solar-weighting against `k25v_hr.dat`. UNBLOCKED: POSEIDON's surface database has  | `notes/audits/orogen-lithology.md` | done -- table rebuilt by solar-weighting measured spectra against k25v_hr.dat. Basalt confirmed, felsic confirmed, quartzite 0.35->0.45, playa 0.30->0.19 |
| LITH-13 | Couple basin-fill albedo to wetness: measured halite crusts swing 0.64->0.24 on wetting, and `surface_water.py | `notes/audits/orogen-lithology.md` | done -- basin-fill albedo is now wetness-coupled: lakes composited from surface_water.nc, and salt crust derived as the zone whose lake depth is at or below local annual evaporation |
| LITH-15 | `playa_clastic` 0.30 sits at the bright end of Post's 52 measured soils (mean 0.189, max 0.402); damp playa is | `notes/audits/orogen-lithology.md` | done -- playa 0.30 -> 0.19, Post et al. (2000) pyranometer mean of 52 soils |
| LITH-17 | Ground or correct the evaporite share: `LITHO_SALT_CRUST_DEPTH_FRAC = 0.25` gives 10.7% of basin fill as salt  | `notes/audits/orogen-lithology.md` | done -- derived rather than tuned. Salt crust is the ephemeral zone from the water balance, 0.47% of land against the geometric rule's 2.85%. Fixed downstream because Orogen has no water balance; its geometric rule still serves erodibility, which runs before any climate exists |
| LITH-18 | Decide where between slab and powder this world's bare rock sits. Paragas slabs are a floor, particulate sets  | `notes/audits/orogen-lithology.md` | done -- resolved by two routes. The pedology model puts regolith at 0.11-3.15 m over 90% of land so the surface is optically regolith, and Longhi's rock-SLAB classes put quartz-rich gneisses at 26-35%, which is where the table already sat. Weathering has no consistent sign, so no single blend fraction exists |
| LITH-1 | Assign evaporite mineralogy downstream from the chemical divide, not from Orogen's single geometric `evaporite | `notes/audits/orogen-lithology.md` | wontfix -- superseded by LITH-2. The brine divide predicts which mineral a basin grows, but albedo should not be split on it |
| REF-1 | NOT HELD: Hardie, Eugster (1970), *The evolution of closed-basin brines*, Special Publications of the Mineralo | `references/INDEX.md` | done -- Hardie & Eugster (1970) obtained, MSA_SP3_273-290.pdf |
| REF-5 | NOT HELD: Hunt & Salisbury, *Visible and near-infrared spectra of minerals and rocks* series, Modern Geology 1 | `references/INDEX.md` | done -- superseded by Hunt (1982) in Carmichael's Handbook, which consolidates the whole Modern Geology series |
| LITH-14 | Split the basalts: a shared 0.10 is the weathered state, fresh lava and tephra are below 0.05 | `notes/audits/orogen-lithology.md` | done -- NO change. 0.10 is correct at 15 km cells: regolith covers 90% of land at 0.11-3.15 m, so the surface is optically regolith (the LITH-18 argument), and fresh lava is a transient state needing active resurfacing the export does not track. Bounded anyway: all 3.01% of land basalt at 0.04 is -0.0018 land albedo, +0.041 W/m2, about +0.02 K. Cannot justify a baseline |
| LITH-19 | Ground albedo and erodibility for the arc and forearc classes, never checked because their assignment rules could not fire | `notes/audits/orogen-lithology.md` | done -- albedo needed no change (arc_andesite 0.20/granite 0.30 = 0.67 against Logan's 0.69). Erodibility corrected: arc_andesite 0.90 -> 0.50, rift_bimodal 0.95 -> 0.50, arc_basalt 0.85 -> 0.65, and the andesite-above-basalt ordering was inverted against Moosdorf. melange kept at 4x. Terrain effect measured and negligible: land mean +0.3 m, -0.00004 land albedo |
| LITH-16 | Rename `melange`, or split blueschist out | `notes/audits/orogen-lithology.md` | done -- renamed to "Subduction melange (block-in-matrix)". The assignment rule produces sheared prism, not blueschist, and it now fires in the forearc where a prism belongs: 0.00% -> 6.17% of land |
| LITH-0 | Determine whether the missing gypsum class is an Orogen oversight | `notes/audits/orogen-lithology.md` | wontfix -- it is not. Orogen assigns evaporite geometrically and has no basis for mineralogy; the decision belongs downstream. Superseded by LITH-1 |
| GRAV-0 | Determine whether unscaled bathymetry is a bug | `notes/audits/orogen-gravity.md` | wontfix -- g cancels in the isostatic balance, so scaling ocean depth would be wrong. The rule is now stated once in `scaledHeightKm` instead of being triplicated |
| GRAV-1 | Decide whether to give Orogen's erosion physical units so gravity enters generation | `notes/audits/orogen-gravity.md` | wontfix -- the erosion law is n = 1, at which post-hoc 1/g scaling and correct-gravity erosion are the SAME operation, and the network is at grade nearly everywhere. Difference is 5.7% of local relief on single-cell headwaters and nothing elsewhere. Superseded by GRAV-4/5/6 |
| GRAV-2 | Re-examine basin statistics for the gravity texture error | `notes/audits/orogen-gravity.md` | wontfix -- rested on the superseded hillslope reasoning. The fluvial scaling is correct, so any basin-size effect must come from glacial overdeepening instead: GRAV-6 |
| GRAV-3 | Revisit the glacier rough pass for the slope error | `notes/glacier-rough-pass.md` | wontfix -- there is no slope error to inherit. The pass keys on relief amplitude, which the 1/g scaling gets right |
| REF-2 | NOT HELD: Selby (1980), *A rock mass strength classification for geomorphic purposes: with tests from Antarctica and New Zealand*, Z. Geomorph. 24, 31-51, `10.1127/zfg/24/1984/31` | `references/INDEX.md` | wontfix -- not needed. Bursztyn 2015 applies Selby's classification at 168 localities and reports the results, so the method is grounded through a paper we hold rather than cited from an abstract. DOI re-confirmed via Crossref: the 1984 in the identifier is the publisher's minting quirk, not an error, and the recorded title was missing its subtitle |
| REF-4 | NOT HELD: Penndorf (1956), DTIC report `10.21236/AD0098766`; DTIC serves a broken TLS chain | `references/INDEX.md` | done -- resolved by a different paper. The supplied page scans turned out to be the BAMS article, *Luminous Reflectance (Visual Albedo) of Natural Objects*, `10.1175/1520-0477-37.4.142`, not the DTIC report. Merged, deskewed and OCRed into references/. Its Table 1 dry/wet pairs corroborate the wetness coupling; it is eye-weighted luminous reflectance, not broadband albedo, and the INDEX row says so. The DTIC report is no longer wanted |
| REF-6 | DOES NOT EXIST: no pyranometer broadband albedo for a gypsum crust, and none for an alkaline trona/natron pan | `notes/audits/orogen-lithology.md` | wontfix -- superseded by LITH-2, which decided NOT to split evaporite by mineralogy: within-mineral spread (halite 0.311-0.877) exceeds the between-mineral difference, and surface state dominates both. The model does not distinguish gypsum from halite, so the measurement it would feed has no consumer. Surface state is handled instead, by the wetness coupling in LITH-13 and the water-balance salt crust in LITH-17 |
| LITH-6 | Decide whether `surface_rock` should be renamed; it carries consolidated lithology and described itself as the exposed surface | `notes/audits/orogen-lithology.md` | done -- renamed to `substrate_class` (and `substrate_class_pre_erosion`), with a description that says what it excludes: surficial cover is derived downstream, not held here. Fork, seven consumers and seven documents updated; `lib/orogen.py` raises a naming error rather than a bare AttributeError on the old name. Free to do because no build had been consumed. The premise needed one correction: `playa_clastic` matches GLiM's unconsolidated share almost exactly by AREA, so what is missing is dune sand and loess outside basins, which is a surficial-cover question (SURF-3) rather than a bedrock one |
| LITH-5 | Explain or correct the composition divergence from GLiM | `notes/audits/orogen-lithology.md` | done -- all of it explained. The volcanic shortfall and most of the metamorphic excess WERE the unreachable arc rules. The evaporite 9.5x is a category error: GLiM's first-level `ev` is a DOMINANCE class, and the paper says evaporites are rarely dominant -- global presence is 3.8% against 0.3% dominant, so 2.85% is 0.75x Earth rather than 9.5x, and the water-balance value from LITH-17 (0.47%) is lower still. The plutonic excess the arc fix created is a separate finding, LITH-20 |
| VOLC-2 | Resolve whether `flood_basalt` and `oib` are andisol-bearing | `pedology/config/pedogenesis.yaml` | done -- WITHOUT the field, which Orogen cannot supply: it has no time axis at all, so a province's age is undefined rather than unmeasured. It does not need one. A LIP emplaces in ~1 Myr and persists for hundreds, and a hotspot chain is active only at its youngest end, so expected value puts a randomly chosen province at ~1% chance of erupting. They are now EXCLUDED on that duration argument rather than reported undetermined; the arc is the exception because it is a boundary process active for as long as the plate subducts |
| VOLC-5 | Apply a glass-dissolution enhancement to vitric terrain | `references/INDEX.md` | wontfix -- it no longer needs placing. VOLC-2 settled that only the arc is resupplied, so vitric terrain is arc terrain, which the andisol model already identifies. What remains is a small correction on a small area, and applying a ~20x field-over-laboratory rate factor to a class the model reaches through a saturating development term would be precision on top of a proxy. Reopen if vitric area grows |
| LITH-20 | The arc root is over-exposed: `granodiorite` reached 7.29% of land against GLiM's intermediate plutonic 0.4%, and 59.5% of the arc belt is stripped to basement | `notes/audits/orogen-lithology.md` | done -- MEASURED, and both proposed causes were wrong. Builds at 1.0/2.0/3.0 km arc cover move granodiorite only 7.29 -> 7.10% and the stripped share 59.5 -> 58.0%. 94.1% of granodiorite cells were assigned arc cover and eroded it to ZERO, while cells that keep cover retain half of it, so arc-belt erosion is bimodal -- under 1 km or over 3 -- and no cover thickness sits in the gap. The root is exposed because arc terrain erodes deeply, which is physical, and there is that much arc terrain because the 100-plate configuration was chosen deliberately. Residual: granite at 11.12% against GLiM's 5.7% is cratonic, untouched by this, and still unexplained |
| LITH-21 | The craton archetype produces almost no gneiss, so undifferentiated basement falls through to granite at 11.12% of land against GLiM's 5.7% acid plutonic | `notes/audits/orogen-lithology.md` | wontfix -- half fixed, half declined. FIXED: craton weight was multiplied by (1-basin), which said a covered craton is not a craton when Earth's are mostly platform, and cover is modelled separately; removed, cratonic basement 1.41% -> 3.46%, adopted in precarve-craton. DECLINED: the residual surface excess. A granite->gneiss relabelling by exhumation depth needs no new class and is cheap -- gneiss is already grounded, and Daly even has granulite density -- but measurement shows it trades divergences rather than reducing them, moving plutonic 2.70x -> 2.29x while pushing metamorphic 1.42x -> 1.63x. Both classes are already high because this world exposes more crystalline basement than Earth full stop, and relabelling changes what the table says rather than how much outcrops. Design recorded in the audit if legibility is ever wanted; p90 is the honest rule |
| MIN-3 | Decide prospectivity versus discrete occurrence | `notes/economic-minerals.md` | done -- PROSPECTIVITY FIELD, a continuous 0-1 favourability. Discrete deposits are sub-grid: a porphyry is 1-2 km against a 15.19 km cell, 0.07-0.13 of one, and a vein is under a hundredth. Placing them here would invent detail the grid cannot hold. They belong with the downscaling pass, which is where GRAV-6 puts glacial overdeepening for the identical reason, and the field is that pass's input |
| MIN-1 | Emit an economic-minerals overlay for the tectonic and magmatic deposit types | `notes/economic-minerals.md` | done -- `minerals/` component, six 0-1 fields per build: porphyry Cu, orogenic gold, VMS, magmatic Ni-Cu-PGE, podiform chromite, kimberlite. Built DOWNSTREAM of the export rather than inside Orogen, because every input but `lipV` is already exported and a value that does not exist during generation cannot feed back into erodibility or albedo -- the no-climate-path rule becomes structural instead of remembered, and a rule change costs no rebuild. Rules live in `minerals/config/prospectivity.yaml`. The exhumation axis is what earns the layer: porphyry forms 1-5 km down and is destroyed by deep erosion while orogenic gold forms 5-15 km down and is revealed by it, so porphyry rejects the 59.5% of the arc belt stripped to its root |
| CLIM-3 | Point the three Penman consumers at a wind that means what the formula needs | `exoplasim/notes/first-output-bin.md`, `notes/failure-modes.md` class 15 | done 2026-08-17. `carve_verdict.py:turbulent_forcing` is now the single reader and the other two call it. Wind comes from the snapshot product, 7.356 m/s against the 8.411 in use, because in the snapshots `spd` is exactly `sqrt(ua^2+va^2)` per sample while the binned `spd` sits between the speed of the mean vector, 3.922, and the mean of speeds -- the model's accumulation cancels the reversing component. Humidity has no snapshot to switch to and does not need one: the binned mean with the corrupt record dropped is 2.1% from the uncorrected one and 0.08% from reconstructing that record by cyclic interpolation. The task text was wrong on two counts, both recorded in the note: the error was 14% and not 3.1%, since the 3.1% was measured on a different run, and the vector averaging is the model's rather than `build_climatology.py`'s |
| HYD-6 | Check the first real verdict against the retain change | `hydrography/notes/retain-fraction.md` | done 2026-08-17. 1,580 carved, 30 marginal, 2,011 preserved on 3,621 basins. All 30 are marginal for discharge and none for uncertainty, which is structural rather than incidental: the margin can only raise a retain and is positive exactly when the incision term is already 1, so `retain` equals `retain_incision` on every basin. No lake-fed spillers at all. The reading that `Q_full` was therefore not worth calibrating was WRONG and is retracted: 30 of 3,621 is not a small residual but a missing landform class, and a factor of four either way on the size of a landform class is not a rounding detail. Earth then falsified the constant outright -- all 29 comparable through-flowing impounded basins get retain 0 under it. See HYD-8 |
| HYD-8 | Replace the discharge-only retain mapping with a depth-normalised one and determine its coefficient | `hydrography/notes/retain-fraction.md`, `notes/no-time-axis.md` | done 2026-08-17. `retain = clip(1 - C * erodibility * Q**0.5 / depth_at_spill, 0, 1)`, C = 161 m per (m3/s)^0.5 with a bracket of 128 to 218. CALIBRATED, not declared: the relaxation window is undefined in a generator with no time axis, so it was settled the way this project settles every missing-time question, by matching the density of Earth's standing through-flowing impounded basins at the size this mesh resolves. Two readings of the same data disagreed by 300x and one size-class error inverted the sign of the conclusion; both are recorded. Verdict 1,512 / 98 / 2,011 against 1,580 / 30 / 2,011 -- the counts barely moved and the composition changed completely, from trickles to deep basins with median depth 1,354 m |
| BIO-3 | Rebuild the LPJ-GUESS binary for the current year length | `biosphere/scripts/build_vesper_header.py` | done 2026-08-17. Header, PFT thresholds and binary regenerated at 183 days against the 181 both existing runs were built with, from the superseded 180.655-day orbit. `VESPER_YEAR_LENGTH_DAYS` sizes arrays, so the stale binary was the silently-wrong case its own header warns about rather than a failing one. Degree-day limits rescale by 0.5005 against 0.4946. Done ahead of BIO-1 on purpose: this half depends on the orbit alone, so it is durable across the carve, while the driver is not |
| DUST-5 | Fit the gust distribution from a high-cadence orbit instead of 32 snapshots | `aeolian/README.md`, `aeolian/scripts/extract_high_cadence_wind.py` | done 2026-08-17. k = 2.012 from 1,463 three-hourly samples against 3.965 from the snapshots, inside Earth's 1.5 to 2.5 and a factor of two from the fit it replaces, exactly as the README predicted it would be. Emission rises 8.8x at the central roughness and the reopening test crosses by 7.4x. Three pyburn defects found on the way, in the extractor's docstring: it cannot read a high-cadence file at all because the time axis is counted from a code that is written twice per timestep, it slurps the whole file, and it wants radius in Earth radii rather than metres |
| DUST-2 | Price the dust radiative forcing, longwave included | `analysis/dust_forcing.json`, `notes/dust.md` | done 2026-08-17. At the DUST-1 burden: shortwave -17.1 W/m2 over ocean to +4.0 over salt crust, longwave +12.6 everywhere as a clear-sky-window upper bound, net -4.5 to +16.7 by surface and +0.3 to +0.9 in the global mean. The sign is surface-dependent and POSITIVE over the bright closed-basin fill, so the earlier shortwave-only 'dust cools this world' is superseded. The decisive number is for DUST-3: switching ExoPlaSim's aerosol on as shipped gives -6.5 to -7.6 W/m2 global mean, an error of 7.6 against 21 W/m2 for the whole stellar sweep that produced 33 K. Also corrected the error budget's claim that aerosols cannot be switched on at all -- they can; the real limitation is that they act in the shortwave only |
| DUST-6 | Put this world's gravity into the saltation threshold | `aeolian/config/dust.yaml`, `references/INDEX.md` | done 2026-08-17, and the exponent is the point. Kok's standardized thresholds are Earth-fitted and his standardization corrects for air density alone. The correction is the FOURTH root of the gravity ratio, 1.0691, not the square root's 1.1429: Shao and Lu (2000) eq 22 splits the threshold into gravity and cohesion terms, the minimum is where they are equal -- Kok et al. (2012) section 2.1 says so of the observed 75-100 um minimum -- and at that minimum `u*t ~ (rho_p g gamma)^(1/4)`, so the cohesion parameter cancels out of the ratio. That is worth having: gamma is known only to a factor of three, 1.65e-4 to 5.0e-4 N/m, and the ratio is 1.0691 to four figures across the whole range. 6.9% on the threshold cut emission 30% and the land-mean optical depth from 0.740 to 0.512. Both papers read and INDEX.md updated |
| DUST-9 | Write the `aerofile`, and pin down the undocumented bottom-level sink | `exoplasim/scripts/dust_aerofile.py`, `aeolian/notes/in-model-dust.md` | done 2026-08-17. The aerofile is generated from `analysis/dust_optics.json` and validated three ways: `radmod` reconstructs the single-scattering albedo and backscatter ratio to four decimals, the optical depth round-trips against the offline chain's 0.5122 exactly, and Qext comes out 2.65, a plausible Mie efficiency for a micron particle. The conversion is the trap -- Qext is an efficiency against one sphere's geometric cross section, so `Qext = 4 apart rhop MEE / 3`, and only the ratio Qext/apart affects optical depth while apart alone sets settling. The sink is NOT undocumented, there is a comment one line above calling it an anti-build-up term, and it is a stability hack rather than a parameterisation: 99% of the bottom layer per timestep, an implied deposition velocity of 52.7 cm/s against a real 0.1 to 2, and it does not scale with dt so the rate is inversely proportional to the timestep. It must be replaced with a deposition velocity before any emission is calibrated |
| DUST-10 | Settle the lake half of dust's effect on the carve | `notes/dust.md`, `analysis/dust_surface_forcing.nc` | done 2026-08-17. Surface forcing is negative everywhere -- -7.8 W/m2 over water, -2.1 even over salt crust where TOA is positive -- because the layer absorbs whatever is beneath it. Applied per cell to `rss`/`rls` before Penman the verdict goes 1,561 -> 1,585 carved: dust dims the lake, evaporation falls, TWENTY-FOUR MORE basins overflow. Direction is the opposite of the TOA reading and the same as the corrected one. Small, because -4.7 W/m2 on a surface net of order 100 is a few percent not ten, and in the RECOVERABLE direction. Ocean validation unchanged at 1.017, which is the check that the perturbation did not break Penman. The catchment term is DUST-11 and is 6-10x larger the other way |
| PHYS-4 | Atmospheric stability in the Penman transfer coefficient | `hydrography/scripts/carve_verdict.py`, `notes/failure-modes.md` class 16 | done 2026-08-17, and the process matters more than the number. IN, using ExoPlaSim's own functions (`fluxmod.f90:236-256`, ECHAM Report 218) and the WATER branch, Miller et al. (1992) free convection, because a lake is water -- a first attempt used the land branch and got magnitude and direction both wrong. The premise was also wrong: closing the surface temperature through the energy balance, a dark lake in bright arid surroundings runs WARMER than the air, so the layer is mostly UNSTABLE and exchange is enhanced, not suppressed. Carve 1561 -> 1539. I first BACKED IT OUT because the ocean ratio widened from 1.0415 to 1.0845, which was tuning physics to a metric that is not truth; class 16 records that. The check that actually tests it is the closure against the model's real sea surface temperature: +0.24 K median error, and the ocean is genuinely unstable at +1.51 K, confirming the branch |
| HYD-13 | `basin_means` matched longitude labels across the Orogen/ExoPlaSim seam | `notes/audits/grid-convention-and-runoff.md` | done 2026-08-17. The mapping is the IDENTITY and the remap was the bug, confirmed by an invariant that can fail: 1.01% of coupling catchment area lands on model-ocean cells index for index against 49.77% remapped. `field_lon` is gone entirely, since there is nothing to reconcile. Verdict 1539 -> 1725 penman carves, 1,172 of 3,621 basins changing, exactly as the audit predicted. `region_grid_cells` in surface_water.py carried the SAME defect -- 7.32% against 50.71% of land mesh area on ocean cells -- and was fixed with it; latitude is still matched, because those axes genuinely differ |
| CONS-2 | Replace the coupling-convention existence assertion with an invariant | `scripts/check_consistency.py` | done 2026-08-17. It asserted that `cell_lon` EXISTED, which no wrong mapping would ever fail, and passed through both occurrences of the defect. Now reports the fraction of catchment area on model-ocean cells and fails above 10%: 1.01% today. A coupling built for another resolution reports a WARNING rather than an OK, because nothing about it has been verified. Class 17 |
| CLIM-8 | Settle which longitude convention is canonical | `exoplasim/scripts/analyze_climatology.py` | done 2026-08-17. The INDEX is canonical and labels are presentation. `shifted()` keeps rotating figures onto the maps/ convention, because a reader comparing a climate figure to a map should not have to think -- but it now says in its own docstring that nothing computed goes through it, and that its existence was once read as evidence a coordinate transform was needed, which is the false premise that put half of every catchment integral over open ocean twice. Axis labels name the convention |
| PHYS-3 | Give the carve verdict's incision coefficient a gravity term | `hydrography/notes/retain-fraction.md`, `notes/audits/orogen-gravity.md` | done 2026-08-17 as NO CHANGE, with the reason recorded. The premise counted `K ~ rho g` without the compensating `S ~ 1/g` that Orogen already applies: `reliefScale` is 1/(g_v/g_e) to sixteen digits, and this project's own gravity audit derives that as the correct steady-state response to `K ~ rho g`. At grade the two cancel exactly, because incision equals uplift and gravity does not enter. For a transient knickpoint the residual is g^(1-n), spanning 0.766 to 1.143 and exactly 1.0 at Orogen's own n = 1, so the bracket is -52 to +13 basins rather than a one-signed 30-40. The gravity term and the slope exponent are ONE question; folded into GRAV-4/5. What survived is HYD-15, that the mapping treats the cut as slope-independent when S and depth are physically the same relief |
| PHYS-5 | Integrate Penman over the diurnal cycle rather than evaluating it at 12-bin means | `hydrography/scripts/carve_verdict.py` | done 2026-08-17. Saturation vapour pressure is convex at about 6.7%/K, so the daily mean of e_s exceeds e_s of the daily mean and a bin-mean evaluation understates the deficit that drives Penman -- Jensen, and one-signed. The range is measured from the model's own `maxt` and `mint`, 8.08 K land-mean and 12.32 K at p90, and a sinusoid between them is integrated at 24 points; `delta` gets the same treatment, since a diurnal-mean e_s with a point-evaluated slope would be inconsistent in the flattering direction. Worth +0.59% on land-mean Penman, about half the +1.2% estimated, because the convexity is on the DEFICIT and Penman's radiative term dilutes it. Carve 1725 -> 1721. DIRECTION: the audit was right and I was wrong -- it raises E and closes basins, the SAME direction as PHYS-4, which only looked opposite while PHYS-4's stable premise still stood. Kept regardless of size: the diurnal cycle exists |
| DUST-1 | Offline dust component at `aeolian/` | `aeolian/README.md`, `notes/dust.md` | done and archived 2026-08-17, having outlived its own residue. SETTLED, and the answer is that this is a dusty world: land-mean optical depth 0.376 at the central roughness against a reopening threshold of 0.10 that was fixed before the answer was known, 5.235 at the smooth end of the shelter bracket and only the roughest end below it at 0.00002, on emission of 14,029 Tg per Earth year against Earth's roughly 2,000. The threshold therefore fired by 3.8x, and what it commits to is that a prescribed field is not defensible. Everything outstanding when it closed belongs to a successor and is tracked there: the radiative pricing to DUST-2, the wind tail to DUST-5, the gravity threshold to DUST-6, the aerofile and the bottom-level sink to DUST-9, the lake and catchment halves of the carve question to DUST-10 and DUST-11, and the in-model port to DUST-3 with DUST-7 and DUST-8 under it. The numbers above are the re-run after HYD-13 corrected the mapping the lake solution is built through; the first measurement of the same quantities gave 0.334 and 12,969 Tg, and four documents were still quoting a 0.512-era figure a day later |
| BIO-4 | Record in `WORKFLOW.md` section 4 that the biosphere reaches the climate through albedo, roughness and forest fraction only, and not through transpiration | `notes/audits/missed-couplings.md` | done 2026-08-17, and VERIFIED in the vendored ExoPlaSim 3.4.2 source rather than asserted. Land evaporation is `mkevap`, and its whole dependence on the land surface is `zkdiff = drhs * zkonst1 * dtransh / dt` at `fluxmod.f90:658`: `dtransh` carries roughness, so that channel is real, and `drhs` at `NVEG = 0` is one bucket, `min(1, dwatc/(0.4*dwmax))`, with the five `dsoilz` layers holding temperature rather than water. No root, canopy, interception or stomatal term exists in `landmod.f90`, `fluxmod.f90` or `surfmod.f90`. Two refinements to the claim as written. Forest fraction is NARROWER than it sounded: at `NVEG = 0` it only mixes the snow albedo's forested and unforested endpoints, so it is radiative and reaches evaporation not at all. And there is a FOURTH channel one step removed, `dwmax` from pedology carrying LPJ-GUESS soil carbon, already bounded near-inert at 1.06x on runoff for a 3.75-fold bucket change, so the word "only" needed it. The finding worth more than the task: switching SIMBA on would NOT supply the missing physics. It does couple back at `nveg == 2`, but `dsc`, its "stomatal conductance", is prescribed -- allocated to `rinidsc` at `simba.f90:157`, optionally read as surface code 1606, and never assigned again -- and `dlai` is computed and used nowhere but output. So the absence is a property of the model, not of this project's configuration |
| CONS-1 | `check_consistency.py`'s biosphere check compares a sha256 of the raw `config/planet.yaml`, so an edited COMMENT is indistinguishable from an edited parameter | `scripts/check_consistency.py` | done 2026-08-17, as ONE mechanism rather than a second one. `config_drift` moved out of `continue_exoplasim.py` into `lib/provenance.py` and takes the allowlist as an argument, so the resume guard and the biosphere check share the comparison but not the list -- reachability is a property of the consumer, and `baseline_climatology` is inert for a resume and live for the biosphere, so a shared list would have to be their intersection. The three biosphere generators now write `source_config` into their provenance, as an ExoPlaSim run manifest already did; a record without it falls back to the file hash and SAYS that it cannot separate a comment from a parameter. Allowlist is four keys, each traced by grep to no reader: `schema_version`, `star.activity`, `star.surface_uv_relative_to_earth`, and the two `stellar_cycle.components.*.note` strings. What was deliberately left OFF is the more useful half and is recorded in the code: `star.spectral_type` looks like a label but `build_stellar_spectrum.py` writes the `.dat` from it, and `model.*` reaches the climatology the solstice offset is fitted against. Both reach an artifact through one more file. It caught a live one on the way: the resume guard's allowlist carried `star.surface_uv`, and the key is `star.surface_uv_relative_to_earth`, so that entry had excused nothing for as long as the guard had existed. `--self-test` is the class 17 half, 8 cases, each negative paired with the positive proving the edit landed, and it was demonstrated failing two ways before being trusted. Left standing knowingly: three binary checks hash source files the same way, but a compiled binary has no value-level view and a rebuild restores certainty rather than silencing a hash. Spun out as CONS-3: the spectrum is staged by name |
| BUDG-3 | Move the error budget's content into `scripts/error_budget.py` and give `analysis/error_budget.json` the same "generated, never edited" rule `world_state.json` has | `notes/audits/missed-couplings.md` | done 2026-08-17, and it was already substantially done: both symptoms the row named were absent, the JSON carried the rule, regeneration was byte-identical and it was tracked. What actually remained is the interesting part, because it was the same convention one level up -- the generator's dust entry NARRATED ITS OWN SUPERSESSION in place, "THE EARLIER SIGN CLAIM IS SUPERSEDED, this entry said...", which is exactly what "rewrite superseded content, do not mark it" forbids. Rewritten to state what is true, with the DUST-3 constraint split into its own priced row |
| BUDG-4 | Make the flux-to-kelvin conversion canonical | `scripts/error_budget.py`, `notes/dust.md`, `WORKFLOW.md` section 5b | done 2026-08-17. `lib/sensitivity.py` owns it and `check_consistency.py` recomputes it from the run index every audit, so it cannot go stale silently again. THE SLOPE IS 202 K per unit flux ratio, 34% above the 150.2 it replaces, corroborated at 201.3 to 203.4 by four further estimates across two terrains. The 150.2 was T21 across 0.95-1.00, a bracket that stopped containing the baseline when the flux moved to 0.945, breaking the rule its own comment stated; and nothing ever fell back to it, `main()` assigned it unconditionally. THE DENOMINATOR WAS THE SECOND ERROR AND COMPOUNDED THE FIRST: a top-of-atmosphere forcing is an absorbed-flux change, so the divisor is `S/4 * (1 - alpha)` = 242.8 W/m2 rather than the incident 321.54, giving 0.832 K per W/m2 against 0.467 in use. Planetary albedo was stale at 0.266 and is now measured from the baseline's own `rst`/`rsut` at 0.28644 rather than declared. Two things worth keeping. The claimed 3.4x gap between the local slope and the stellar sweep's sensitivity was 1.9x once the local conversion was right, so most of it was arithmetic rather than the ice transition -- though the sweep is still the wrong sensitivity to use at the baseline. And the ORDERING moved: nothing reorders inside the albedo column, where a slope is a uniform multiplier, but the W/m2 items that previously sat under "no conversion invented" now have kelvin, and shortwave-only in-model dust lands SECOND in the whole budget at 3.3 to 3.6 K. The trap avoided: pairing the baseline climatology with the 0.910 run crosses a geography change and yields 224, a +0.80 K surface revision wearing a slope's units, so the bracket is taken within one geography and `verify()` enforces it |
| CLIM-2 | The first output bin of every orbit has corrupted WIND and HUMIDITY only, worsening downward from 1.02 at the model top | `notes/failure-modes.md` class 14 | done 2026-08-17, mechanism PINNED to six figures and now class 18. Three defects in the `NLOWIO = 1` path, and the counter was NOT the cause. `plasim.f90:857` and `:1086` save six SPECTRAL accumulators through `mpputgp`/`mpgetgp`, whose dummy is declared `pp(NHOR,klev)`; Fortran reinterprets the `(NESP,NLEV)` array and moves 5,120 of 19,040 elements at T42/L10/p16, so levels 4-10 are restored as ZERO while `naccuout` survives intact at 90 and the first write of each call divides 159 samples by 249. `outmod.f90:394` then subtracts the whole `plavor` from an accumulator holding only 0.638554 of it, leaving `-(1-delta)*plavor` in the (n=1,m=0) vorticity mode -- solid-body rotation, a retrograde 160.8*cos(phi) m/s on levels 4-10. That is the entire wind signature, and it explains every part of it: `ua` only because plavor lives in vorticity, worsening downward because the spurious wind is level-independent while the real one falls, and temperature escaping because `aast` holds the deviation from `t0`. Humidity is separate: `sqout` is recomputed only at output steps but summed every step and is not in the restart, and since 5850 mod 160 = 90 its phase walks with a 16-orbit period, measured at 0.946 correlation over 90 orbits. PREDICTIONS STATED BEFORE MEASURING, which is what makes this a mechanism rather than a story: the surviving coefficients must show no anomaly and the truncated ones must all show the same one, giving delta = 159/249 to six figures with a relative residual of 3e-7; a 6018-step run must give a DIFFERENT constant, predicted -0.127108 and measured -0.127108; orbit 0 of every run must be clean, and it is; and the wind anomaly must be a level-independent `cos(phi)` solid body of -53.59 m/s with nothing fitted, measured -53.44 to -53.56 on levels 3-9. The trap named in the task, `naccuout` persisting while the accumulators do not, was ELIMINATED with evidence: it is restored correctly, and the gridpoint accumulators share the same counter and are clean |
| PHYS-2 | Make the star's band-1 flux share canonical. Three values were in use: 0.3777 from `k25v.dat`, 0.3862 from `k25v_hr.dat`, and 0.4184 printed by the model | `notes/audits/physics-review.md` finding 2 | done 2026-08-17, and it was NOT an integration question. THE MODEL'S 0.4184 IS NOT THIS STAR: `radmod.f90:813` takes the spectrum branch only when `NSTARFILE > 0`, and no run's `radmod_namelist` carries it -- all four have `STARBBTEMP = 4965.0`, `NSTARTEMP = 1` and no `STARFILE` -- so `solarini` has been building a 4965 K Planck curve and printing its partition. `lib/stellar.py` reproduces `solarini` and matches every printed digit: 0.418350 for the blackbody against the model's 0.418350399, 0.384383 for `k25v_hr.dat`, and 0.517000 for the 5772 K identity `radmod.f90:207` states. The mechanism is that `exo.Earthlike(workdir=...)` re-copies the shipped namelists, and both `continue_exoplasim.py` and `run_stellar_cycle.py` reconstruct the model on an existing run without `starspec`; `run_exoplasim.py` was the only driver that staged the spectrum and it runs one orbit, so `MOST_DIAG.00000` of every run reports 0.384383 and every file after it reports 0.418350. Worth +0.024 on broadband snow albedo, double the 0.013 estimated, and it moves sea ice, glacier, ground and ocean albedo too. Canonical value is 0.384383, computed rather than stored, with `check_consistency.py` failing when a copy drifts. THREE THINGS FOUND ON THE WAY, all larger than the task. `solarini` integrates its 5772 K Rayleigh reference over the WRONG array, having overwritten `wv1`/`wv2` with the file's wavelengths first, so `rcoeff` is 0.209732 against 0.712517 -- which inverts the fix order, since declaring the spectrum without patching that would weaken Rayleigh scattering 3.4x. `dust_forcing.py` carried a FOURTH value, 0.2566, from counting the bin width twice. And the physics review's water-vapour ratio of 1.204 was read off the blackbody, so PHYS-1 is worth +9 to +12 W/m2 rather than +6.5 to +9. Spun out as SPEC-1 and SPEC-2 |
