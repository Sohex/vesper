# Run identity: forty runs this project cites and cannot identify

*Worldbuilding frame: bookkeeping about the Vesper project's climate model runs,
an ExoPlaSim fork run on this desktop. Nothing here is about the simulated
planet. Measured 2026-08-30 at 1068ff073.*

A run id is `run_` and twelve hex digits. That is deliberate -- CLAUDE.md rule 6,
because a parameter-built name separates runs only along the dimensions it
encodes and both ExoPlaSim and LPJ-GUESS collided that way -- and it puts the
entire burden of "what was this run" onto a record. Rule 6 names the record:
`exoplasim/runs/INDEX.json`. An id with no record is a dead string, and a claim
that cites one is a claim nobody can check.

## The defect

`exoplasim/runs/INDEX.json` was **rebuilt by scanning `exoplasim/runs/`**. The
identity of a run lives in a `run_manifest.json` inside its own directory;
`exoplasim/runs/` is untracked and the manifest dies with the payload. So the
index recorded what EXISTED at the moment of the scan rather than what had
existed, and two things followed from that:

- a run created and deleted between two scans left nothing tracked anywhere;
- a rescan after any deletion ERASED the row of a run that had been indexed.

`scripts/archive_runs.py` is the deliberate deletion path and it does extract an
`INDEX_ENTRY.json` before deleting. It was never the leak. It could only ever
preserve a row that had been captured while the payload was on disk, which made
it the only thing standing between a deleted run and a dead string, and a hand
`rm -rf`, a crash during preparation, a purge and a run that simply never got
reindexed all bypassed it.

## The size of it

Forty run ids this tree cites are in no record: not in `INDEX.json`, not an
`INDEX_ENTRY.json` stub, not an `INDEX_AT_DELETION.json` batch, not a
`RECONSTRUCTED.json`. Thirty-three of them are cited by a note or an audit, and
the remaining seven only by analysis artifacts. Every one of them was a directory
under `exoplasim/runs/`, because the id shape is what `run_exoplasim.py` emits
and nothing else emits it.

`archive/runs/RECORDLESS.json` is the enumeration with the surviving citations
per id, generated by `python exoplasim/scripts/index_runs.py --recordless`. It is
generated rather than listed so that it shrinks by itself as identity is
recovered, and so that it measures the remaining damage instead of describing a
moment.

What is gone for each of them: the configuration, the config sha, the executable
sha, the source build, the orbit count, the namelists, the convergence
assessment, and every field the manifest carried. What survives is whatever an
analysis artifact happened to copy out before the run went.

## What the loss costs, by shape

The runs divide by how much of them an artifact preserved, and the three shapes
have different consequences.

**The measurement survives and the provenance does not.** `run_8044646ea7f0`
kept `exoplasim/analysis/convergence/run_8044646ea7f0_convergence.json` with all
85 annual records, so the convergence-criterion sweep in
`notes/audits/resolution-ladder.md` is re-derivable in full. What cannot be
recovered is which build it ran on and which executable integrated it, so the
sweep cannot be attributed to a source state and cannot be paired with a
controlled arm.

**Nothing survives but a summary row.** `run_4fe5e6050df5` appears in
`exoplasim/analysis/filter_spectral_cost_arms.json` as T42 at dt 22.5, gamma 16,
and in a restart template's provenance. No artifact carries its temperature, so
the 294.567 K that the resolution-ladder audit's -1.300 K resolution step rests
on is a number that note is the sole copy of. It can only be replaced by running
a T42 arm at dt 22.5 again.

**Enough survives to reconstruct, and reconstructing falsified an audit.**
`run_1d39fef9bfc2`, `run_42aaf441b10b` and `run_900548ae632e` have
`RECONSTRUCTED.json` files under `archive/runs/`, each citing the artifact behind
every field and listing what is unrecoverable. They are named apart from
`INDEX_ENTRY.json` because they are not copies of a real index row. The
reconstruction recovered two NAMELISTS out of
`exoplasim/analysis/filter_dt_pair.json`, and they showed the two ladder arms
differed in `NFILTEREXP`, 16 against 8, where the audit said they differed only
in where they started. A finding that had stood for six days was wrong, and only
a reconstruction of runs that no longer existed could say so.

## What closed it

`exoplasim/runs/INDEX.json` is a LEDGER of every run that has existed. Two
mechanisms hold that, and both are needed:

- **register at creation.** `run_exoplasim.py` calls `index_runs.register` the
  moment the manifest is written, before a single orbit, and again when the block
  ends however it ends. `continue_exoplasim.py` re-registers after each segment.
  A run therefore has a tracked row from the moment it exists, so the window in
  which a deletion could erase it entirely is closed rather than narrowed. It is
  never fatal: a bookkeeping error must not cost a model run, so a failure warns
  with the command to run by hand.
- **merge, never replace.** A rescan updates the rows it can see and keeps every
  row it cannot. A vanished directory keeps its last known row untouched and
  gains `payload_present: false`, the date the disappearance was first noticed,
  and whether an `archive/runs/` stub holds its extracted products.

`scripts/smoke_test.py:check_run_index_is_a_ledger` refuses the regression on an
identity rather than a comparison: a merge against a scan that sees nothing must
return every row it was given with its identity fields untouched, and a scan that
does see a run must still win for that run's live fields, so a ledger that simply
ignored the scan fails the same gate.

A blow-up registers too, and that is not incidental. `lib/rungs.py` reads a
failed status as a ladder verdict, and `run_900548ae632e`'s SIGFPE in its
forty-seventh orbit is a row nothing can check precisely because the run that
took it left no record. A run that dies is exactly the run least likely to be
reindexed by hand.

## What is not closed

The forty. Registration prevents the next one; it recovers none of these, and
most of them cannot be recovered at all. The disposition per run is either a
`RECONSTRUCTED.json` where enough survives to piece one together, or the honest
statement that the claim resting on it cannot be checked -- which is what
`notes/audits/resolution-ladder.md` now says of its own rows.

Also open: `maps/` uses the same UUID-plus-tracked-index pattern for rendered
frames, with `maps/data/<build>/INDEX.json` written from what is on disk. Whether
it has the same shape of defect is not measured here.
