#!/usr/bin/env python3
"""Build the OCN-11 wet support, bathymetry, topology, and remap bundle."""

from _pending import pending_main


if __name__ == "__main__":
    pending_main(
        "OCN-11 (blocked by SPAT-1, SPAT-5, and SPAT-10)",
        "Build the versioned ocean spatial-support contract from the active Orogen export.",
    )
