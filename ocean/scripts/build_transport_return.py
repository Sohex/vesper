#!/usr/bin/env python3
"""Build conservative ocean heat-convergence and surface-velocity returns."""

from _pending import pending_main


if __name__ == "__main__":
    pending_main(
        "OCN-10/OCN-11 (forcing and support contracts are not accepted)",
        "Map the accepted ocean return fields onto ExoPlaSim support.",
    )
