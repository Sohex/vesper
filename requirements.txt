# Project-level direct dependencies. Install with:
#   UV_CACHE_DIR=/tmp/world-uv-cache uv pip install --python .venv/bin/python -r requirements.txt
# exoplasim is NOT installed from PyPI. It is the vendor/exoplasim subtree,
# installed editable so a build compiles in place:
#     uv pip install --python .venv/bin/python -e vendor/exoplasim
# See CLAUDE.md. Pinning a version here would reinstall upstream over it.
matplotlib==3.11.1
netCDF4==1.7.4
numpy==2.5.2
pillow==11.3.0
PyYAML==6.0.3
scipy==1.18.0
xarray==2026.7.0
# Static analysis for scripts/smoke_test.py: F821 undefined names are a
# crash class that answering --help cannot detect.
pyflakes==3.4.0

# Earth reference data for the lake solver validation, HYD-4. geopandas and its
# stack read HydroBASINS and HydroLAKES shapefiles and do the point-in-polygon
# join that finds each endorheic system's terminal lake.
geopandas==1.1.4
pyogrio==0.13.0
pyproj==3.7.2
shapely==2.1.2
# Open-Meteo's .om format, on the AWS open-data mirror. Reads by HTTP range
# request rather than downloading a chunk, which is the only sane way in: an
# ERA5 chunk is 95 MB for 21 days. Note the mirror is a ROLLING window of about
# 4.6 years, not the 1940-onward archive, so it cannot serve a 1991-2020 normal.
# Reads Copernicus DEM COG tiles out of S3 without downloading them.
rasterio==1.5.1
omfiles==1.2.0
fsspec==2026.7.0
s3fs==2026.7.0

# The Earth calibration's benchmark stage, which measures what the RESOLVABLE
# FIELDS support and so sets the bar the physical model is judged against.
# `hydrography/scripts/earth_calibration.py --stage benchmark`. A linear fit
# understates that bar badly, 0.13 against 0.21 held out, so the flexible
# arm is not a convenience: the number it produces is the criterion.
scikit-learn==1.9.0
